# CLAUDE.md

คำแนะนำสำหรับ Claude Code เมื่อทำงานในโปรเจกต์นี้

## สิ่งนี้คืออะไร

ระบบแผนที่เก็บข้อมูลแปลงที่ดินและต้นไม้ (`ระบบแผนที่ต้นไม้`) เป้าหมายระยะยาวคือใช้ประกอบระบบ carbon credit

**ตอบกลับผู้ใช้เป็นภาษาไทยเสมอ** ไม่ว่าผู้ใช้จะพิมพ์ภาษาอะไรมาก็ตาม (โค้ด/commit message/ชื่อไฟล์ใช้ภาษาอังกฤษได้ตามปกติ)

## สถานะปัจจุบัน

ฝั่ง client ยังเป็นเว็บแอปไฟล์เดียว ไม่มี build step — เขียน HTML/CSS/JS ล้วนในไฟล์เดียว ใช้ [Leaflet.js](https://leafletjs.com/) (โหลดผ่าน CDN พร้อม fallback หลายแหล่ง) แผนที่ default เป็น **ภาพถ่ายดาวเทียม (Esri World Imagery)** สลับไปแผนที่ถนน (OpenStreetMap) ได้ผ่าน layer control มุมล่างขวา **แต่ตั้งแต่ v1.8 มี backend จริงแล้ว** (ก่อนหน้านี้ไม่มี)

- ไฟล์หลัก: `index.html` (เวอร์ชันล่าสุด v1.27)
- `tree-map-system-1.html`: โค้ดคนละเวอร์ชัน/prototype เก่ากว่า (ไม่มีระบบ fallback error handling) ไม่ชัดเจนว่ายังใช้งานอยู่หรือไม่ ถามผู้ใช้ก่อนแก้ไฟล์นี้
- **Backend (v1.8)**: Node.js + Express (`server.js`, `server/app.js`, `server/db.js`) + Postgres (`schema.sql`) — REST API ที่ `/api/plots` และ `/api/trees` (GET ทั้งหมด, `PUT /:id` = upsert, `DELETE /:id`) ลบแปลงจะ cascade ลบต้นไม้ในแปลงนั้นด้วย (FK `ON DELETE CASCADE`) โครงสร้างตารางไม่ใช้ PostGIS ยังเก็บพิกัดเป็น column ธรรมดา/JSONB (ยกไป PostGIS ทีหลังได้ถ้าต้อง query เชิงพื้นที่ซับซ้อนขึ้น)
- **Admin dashboard**: เว็บแอปแยก `admin/` (Vite + React + Tailwind v4 + react-leaflet + react-router) build แล้ว serve จาก Express เดิมที่ path `/admin` — ดูรายละเอียดที่หัวข้อ "Backend / Deploy" ด้านล่าง
- **Deploy**:
  - **Render** (`render.yaml` blueprint) — deploy เป็น Web Service เดียวที่ serve ทั้ง `index.html`, `/admin` (React build), และ API พร้อม Render Postgres แนบอัตโนมัติผ่าน `DATABASE_URL` นี่คือ deploy target หลักตอนนี้ (ผู้ใช้ต้องเชื่อม GitHub repo กับ Render เอง — Claude สร้างบัญชี/กด deploy ให้ไม่ได้) `buildCommand` รัน `npm run build` ซึ่ง build ทั้ง root deps และ admin app ให้อัตโนมัติ
  - GitHub Pages จาก branch `main` → https://taro191.github.io/tree-map/ ยังทำงานอยู่แต่เป็น **static เท่านั้น** ไม่มี backend รองรับ ดังนั้น `/api/*` จะ fail และแอปจะโหลดเป็นค่าว่าง (มี error banner แจ้งเตือนอยู่แล้ว) — เก็บไว้เป็น mirror/demo หน้าตาเฉยๆ ไม่ใช่ที่ใช้งานจริงอีกต่อไป
- **เก็บข้อมูล**: ผ่าน REST API ข้างต้น ลง Postgres จริง (`window.storage` shim เดิมถูกถอดออกหมดแล้วตั้งแต่ v1.8)
- **Auth**: `/api/plots` GET, `/api/trees` GET และการแก้ไข/ลบแปลง-ต้นไม้ที่มีอยู่แล้วใน `index.html` (แอปภาคสนาม) **ยังเปิดสาธารณะไม่มี auth เหมือนเดิมโดยตั้งใจ** อย่าเพิ่ม auth เข้าไปที่ endpoint พวกนี้โดยไม่ถามผู้ใช้ก่อน เพราะจะทำให้แอปภาคสนามพังทันที — **ยกเว้น** ปุ่ม "+ เพิ่มแปลงใหม่" (v1.13) ที่ตอนนี้บังคับระบุตัวตนก่อน (ดูหัวข้อ v1.13 ด้านล่าง) นี่คือ auth เดียวที่มีอยู่ในฝั่งแอปภาคสนาม ไม่ครอบคลุมส่วนอื่น (ดูแปลง/ต้นไม้ที่มีอยู่, เพิ่มต้นไม้ในแปลงเดิม, แก้ไขแปลง/ต้นไม้ ยังไม่ต้อง login) ระบบสมัครสมาชิก admin เปิดให้สมัครเองได้อิสระ (ไม่ใช่ invite-only) ตามที่ผู้ใช้เลือกไว้ — หมายความว่า **ใครก็ตามที่รู้ URL /admin/register ก็สมัครเป็น admin ระบบเต็มรูปแบบได้ทันที** (คุยกับผู้ใช้แล้ว เป็นการตัดสินใจที่ตั้งใจ ไม่ใช่ oversight) — **สำคัญ**: ตาราง `users` ใช้ร่วมกันระหว่างระบบ admin login กับระบบระบุตัวตนของแอปภาคสนาม (v1.13) ตามที่ผู้ใช้เลือกไว้เอง เป็น**ตารางเดียวกัน**ไม่ใช่แยกกัน แต่ตั้งแต่มีระบบ role (ดูหัวข้อ "ระบบ role/สิทธิ์การเข้าถึง" ด้านล่าง) **field worker ที่ลงทะเบียนผ่านแอปภาคสนามจะได้ role เป็น `user` และเข้า `/admin` ไม่ได้เลย** (ต่างจากพฤติกรรมเดิมก่อนมี role system ที่ยัง login เข้า `/admin` ได้ด้วยเบอร์โทร+รหัสผ่านวันเกิด) ส่วนคนที่สมัครผ่าน `/admin/register` ตรงๆ ยังได้ role เป็น `admin` เหมือนเดิมทุกประการ (ไม่กระทบ)
- **ระบบ role/สิทธิ์การเข้าถึง** (`users.role`, ค่าเป็นหนึ่งใน `user` | `admin` | `enterprise_admin`, default `'admin'` ที่ระดับ column เพื่อไม่กระทบบัญชี admin เดิมที่ deploy อยู่แล้ว): `user` = ผู้ใช้ทั่วไป (field worker ที่ลงทะเบียนผ่านแอปภาคสนาม) เข้า `/admin` ไม่ได้เลย, `admin` = สิทธิ์เต็มทุกอย่าง, `enterprise_admin` = ดูแลเฉพาะวิสาหกิจชุมชนกลุ่มเดียวที่ผูกไว้ผ่าน `users.managed_community_enterprise_id` (FK ไป `community_enterprises`, `ON DELETE SET NULL`) — แก้ไขข้อมูลกลุ่มตัวเอง/เพิ่ม-ลบสมาชิก/export CSV-GeoJSON ที่ scope เฉพาะแปลง-ต้นไม้ในกลุ่มตัวเองได้ แต่**ลบกลุ่มตัวเองไม่ได้** (ต้องเป็น `admin` เท่านั้น แม้กลุ่มจะไม่มีสมาชิกเหลือแล้วก็ตาม) และสร้าง user ใหม่/เลื่อนสิทธิ์คนอื่นไม่ได้ — client-supplied `fieldRegistration:true` flag ที่ `POST /api/auth/register` **กำหนด role ได้แค่ทางเดียวคือลดสิทธิ์ลงเป็น `user`เท่านั้น ไม่มีทางขอ `admin`/`enterprise_admin` ผ่าน endpoint สาธารณะนี้ได้** (ตั้งใจกันการ privilege escalation) การให้สิทธิ์ `admin`/`enterprise_admin` ทำได้ทาง 2 ทางเท่านั้น: `POST /api/admin/users` (สร้างใหม่พร้อม role) หรือ `PATCH /api/admin/users/:id/role` (เปลี่ยน role ของ user ที่มีอยู่ ต้อง `requireAdmin` ทั้งคู่ — ทั้งสอง endpoint validate ว่า `managedCommunityEnterpriseId` ต้องมีและมีอยู่จริงถ้าจะตั้งเป็น `enterprise_admin`) — **role ถูกฝังไว้ใน JWT ตอน login** (`server/auth.js` `signToken`) ดังนั้นถ้า admin เลื่อน/ลดสิทธิ์ user คนหนึ่งผ่าน `PATCH .../role` ผลจะยังไม่มีผลจนกว่า user คนนั้นจะ logout/login ใหม่ (หรือ token 7 วันหมดอายุ) — เป็น trade-off ที่มากับดีไซน์ stateless JWT เดิม ไม่ต้องแก้ ถ้าต้องการ effect ทันทีให้บอกผู้ใช้ที่ถูกเปลี่ยน role ให้ logout/login ใหม่เอง — middleware `requireAdmin`/`requireAdminOrEnterpriseAdmin` อยู่ใน `server/auth.js`, helper `isOwnCommunityEnterprise()`/`scopePlotsForUser()` ใน `server/app.js` ใช้ scope route ที่เกี่ยวกับ community enterprise/export ให้ enterprise_admin เห็นเฉพาะของตัวเอง — **`/api/plots`, `/api/trees` (public endpoints เดิม) ไม่ถูกแตะต้องเลย** ยังเปิดสาธารณะไม่มี scope ตาม role เหมือนเดิมทุกประการ — ฝั่ง `admin/` React app: `ProtectedRoute.jsx` เช็ค `role==='user'` แล้วบล็อกเข้าไม่ให้เห็น dashboard เลย (มีแค่หน้าข้อความ "ไม่มีสิทธิ์เข้าถึง" + ปุ่มออกจากระบบ), `Dashboard.jsx` กรอง plots/trees ฝั่ง client ให้เหลือเฉพาะของกลุ่มตัวเองถ้า role เป็น `enterprise_admin` (จำเป็นต้องกรองฝั่ง client เพราะ `/api/plots`/`/api/trees` เป็น public endpoint ที่ไม่ scope ให้) และซ่อน `UsersPanel` ถ้าไม่ใช่ `admin`, `CommunityEnterprises.jsx`/`CommunityEnterpriseCard.jsx` ซ่อนฟอร์ม "+ เพิ่มกลุ่ม" และปุ่ม "ลบ" ถ้าไม่ใช่ `admin`, `UsersPanel.jsx` (เห็นเฉพาะ `admin`) เพิ่ม dropdown เลือก role + กลุ่มที่ดูแล ต่อ user แต่ละคน เรียก `PATCH /api/admin/users/:id/role` — ทดสอบ backend ครบใน `test/roles.test.js`, ทดสอบ end-to-end ผ่านเบราว์เซอร์จริงแล้ว (field registration → role `user` → บล็อก `/admin`, admin เลื่อน user เป็น `enterprise_admin` → login ใหม่ → เห็น/แก้ไขได้เฉพาะกลุ่มตัวเอง ลบกลุ่มตัวเองไม่ได้)
- **Dev ท้องถิ่น**: `npm install` แล้ว `npm run dev` รันเซิร์ฟเวอร์เดียวกันแต่สลับไปใช้ in-memory store (`scripts/dev-memory-server.js`) ไม่ต้องมี Postgres จริงในเครื่อง เหมาะกับทดสอบเร็วๆ (auto-set `JWT_SECRET` dev-only ให้ด้วย) ส่วน `npm test` รัน integration test ผ่าน store เดียวกันนี้ด้วย `node --test` — ถ้าจะแก้ไฟล์ใน `admin/` ระหว่าง dev ต้อง `npm run build --prefix admin` ใหม่ก่อน (หรือ `npm run dev:admin` เพื่อรัน Vite dev server แยกที่ proxy `/api` ไปที่ `localhost:3000`)

## ฟีเจอร์ที่มีแล้ว

- เพิ่มแปลงที่ดิน (ชื่อ, เจ้าของ, เบอร์ติดต่อ, สีประจำแปลง)
- วาดขอบเขตแปลงบนแผนที่ (คลิกทีละจุด หรือโหมดเดินสำรวจด้วย GPS)
- ปักหมุดต้นไม้ในแปลง พร้อมเลขลำดับอัตโนมัติต่อแปลง (คลิกบนแผนที่ หรือใช้ตำแหน่ง GPS ปัจจุบัน)
- บันทึกชื่อ/สายพันธุ์, ลิงก์รูปภาพ, หมายเหตุต่อต้นไม้
- ดีไซน์แบบ bottom sheet สำหรับมือถือ
- **(v1.5)** ฟิลด์ข้อมูลแปลงเพิ่มเติม: ประเภท/เลขที่เอกสารสิทธิ์, เนื้อที่ (ไร่/งาน/ตารางวา), ที่ตั้ง (อำเภอ/จังหวัด/รหัสไปรษณีย์)
- **(v1.5)** ถ่ายรูปแปลงพร้อมประทับข้อมูลลงรูปอัตโนมัติ (ชื่อเจ้าของ, เอกสารสิทธิ์, เนื้อที่, ที่อยู่, วันเวลา, พิกัด UTM จาก GPS ขณะถ่าย) สไตล์ "GPS Map Camera" — เก็บได้แปลงละ 1 รูป (ถ่ายใหม่จะแทนที่รูปเดิม) แปลงพิกัด lat/lon เป็น UTM ด้วยฟังก์ชันในตัว ไม่พึ่ง library ภายนอก ฟังก์ชันหลัก `stampPlotPhoto()` — สไตล์ตัวอักษรอัปเดตทีหลังหลายรอบ (ยังใน v1.5 flow เดิม ไม่ได้บันทึกเป็น version ใหม่ทุกรอบ) เริ่มจากตัวหนังสือขาวบนแถบไล่สีดำโปร่งแสง → ตัวหนังสือดำหนาทึบวางตรงบนรูป + เงาขาวจางๆ (ไม่มีแถบพื้นหลังแล้ว) → **ล่าสุด (v1.26) กลับมาเป็นตัวหนังสือสีขาว + เงาสีดำ** (`ctx.fillStyle='#fff'`, `ctx.shadowColor='rgba(0,0,0,0.85)'` ใน `stampPlotPhoto()`) ตามที่ผู้ใช้ขอ อ่านออกชัดบนพื้นหลังสว่าง/รูปกลางแจ้งได้ดีกว่า บล็อกล่างซ้าย (ที่อยู่+วันเวลา) และล่างขวา (พิกัด UTM) จัด bottom-aligned คือ baseline ของบรรทัดสุดท้ายเสมอกันทั้งสองฝั่ง (ไม่เปลี่ยน)
- **(v1.6)** ถ่าย/อัปโหลดรูปเอกสารที่ดิน (โฉนด/สปก./นส.3 ฯลฯ) แยกจากรูปแปลง — ประทับลายน้ำ "ใช้ประกอบเรื่องคาร์บอนเครดิตเท่านั้น" แบบเอียง 30° กระจายเต็มภาพอัตโนมัติ (กันการนำรูปเอกสารไปใช้นอกวัตถุประสงค์) เก็บได้แปลงละ 1 รูปเช่นกัน
- **(v1.7)** เนื้อที่ไร่จำกัดไม่เกิน 30 ไร่ (validate ทั้ง `max` attribute และตอนกดถัดไป) ปรับช่องเนื้อที่ให้แคบลง (ไร่/งาน/ตร.วา) และเปลี่ยนฟิลด์ที่ตั้งแปลงเป็น: กรอกรหัสไปรษณีย์ 5 หลัก → ระบบค้นหาอำเภอ/จังหวัดที่ตรงกันให้อัตโนมัติ (auto-fill ถ้าเจอที่เดียว, ให้เลือกจาก dropdown ถ้าเจอหลายที่ เช่น กรุงเทพฯ ที่บาง postcode คาบเกี่ยวหลายเขต) — ใช้ข้อมูลจาก [kongvut/thai-province-data](https://github.com/kongvut/thai-province-data) โหลดผ่าน `fetch` จาก raw.githubusercontent.com ตอน focus ช่องรหัสไปรษณีย์ครั้งแรก (ต้องมีอินเทอร์เน็ต, มี fallback ให้กรอกอำเภอ/จังหวัดเองถ้าโหลดไม่สำเร็จ) ฟังก์ชันหลัก: `loadThaiZipIndex()`, `handlePostcodeInput()`
- **(v1.8)** ย้ายจาก `window.storage` shim ไปใช้ backend จริง (Node/Express + Postgres บน Render) — ดูรายละเอียดที่หัวข้อ "Backend / Deploy" ด้านล่าง
- **(v1.9)** ย้ายส่วนรูปแปลง+รูปเอกสารที่ดิน (`renderPlotDetail()`) จากแท็บ "ต้นไม้" ไปแท็บ "แปลงที่ดิน" แทน (แสดงใน `#plot-detail` ต่อจากรายการแปลง) แท็บ "ต้นไม้" เหลือแค่ banner ข้อมูลแปลง + โหมดเพิ่มต้นไม้ ไม่มีรูปภาพอีกต่อไป
- **(v1.10)** เปลี่ยนจากระบบ 2 แท็บ ("แปลงที่ดิน"/"ต้นไม้" แสดงพร้อมกันทุกอย่างในแท็บเดียว) เป็น 4 หน้าจอแบบ stack navigation ผ่าน `setView(name)` (toggle class `.active` บน `.panel` ที่มี id `view-<name>`, ไม่มี `.tabs`/`.tab-btn` อีกต่อไป): `view-home` (รายการแปลงล้วนๆ + ปุ่ม "+ เพิ่มแปลงใหม่" — ไม่มีฟอร์มหรือรูปแปะอยู่ในหน้านี้แล้ว) → `view-plot-form` (ฟอร์มเพิ่มแปลงใหม่ ข้อมูล+วาดขอบเขต แสดงเดี่ยวๆ เต็มจอ กด "← กลับ" ยกเลิกกลับหน้าแรกได้ทุกเมื่อ แม้กำลังวาดขอบเขตอยู่ก็ยืนยันก่อนทิ้งข้อมูล) → กดเลือกแปลงจากรายการเข้า `view-plot-detail` (ข้อมูลรายละเอียดแปลง + รูปแปลง/รูปเอกสารที่ดิน จาก `renderPlotDetailInfo()`/`renderPlotDetail()` + ปุ่ม "+ เพิ่มต้นไม้" อยู่ด้านบน) → กดปุ่มนั้นเข้า `view-trees` (โหมดเพิ่มต้นไม้/รายการต้นไม้ในแปลง) บันทึกแปลงสำเร็จจะพากลับ `view-home` เสมอ (ไม่ auto-jump ไปหน้าเพิ่มต้นไม้เหมือนเดิม) และตอนเปิดแอปใหม่จะไม่ auto-select แปลงแรกให้อีกต่อไป (หน้าแรกว่างจนกว่าจะกดเลือกเอง) ผู้ใช้ตัดสินใจเรื่องนี้ไว้ชัดเจนหลังคุยเรื่อง trade-off ปุ่มเมนู ☰ ย้ายจากขวาบนไปซ้ายบนแทน (เดิมชนกับปุ่ม zoom +/- ของ Leaflet ที่ปักหมุดซ้ายบนเป็นค่า default — แก้ด้วยการเพิ่ม `padding-left` ให้ `#topbar` เผื่อพื้นที่ ไม่ได้ย้าย Leaflet zoom control) กดแล้วเปิด dropdown เล็กๆ (`#menu-dropdown`) มี 2 รายการ: "กลับหน้าแรก" (`setView('home')` ตรงๆ ไม่ผ่าน confirm ใดๆ) และ "ย่อ/ขยายแผงข้อมูล" (เรียก `toggleSheet()` เดิม) ปิด dropdown เองเมื่อคลิกนอกพื้นที่เมนู — **ระวัง**: ถ้าจะย้ายปุ่มลอยอื่นๆ ไปมุมซ้ายบนอีกในอนาคต ต้องเช็คตำแหน่ง `.leaflet-control-zoom` (อยู่ที่ x:10-44, y:10-74 โดย default) ก่อนเสมอ กันชนซ้ำ
- **แผนที่ภาพถ่ายดาวเทียม**: แผนที่หลัก (`#map`) default เป็น Esri World Imagery (`SATELLITE_TILE_URL`/`SATELLITE_ATTRIBUTION` ประกาศไว้ตัวเดียวใกล้ `COLORS` แล้ว reuse ทั้งแผนที่หลักและแผนที่ย่อในหน้ารายละเอียดแปลง) มี `L.control.layers(...)` มุมล่างขวาให้สลับกลับไปแผนที่ถนน (OpenStreetMap) ได้ ไม่ต้องใช้ API key (Esri World Imagery ใช้ฟรีไม่ต้องสมัคร ต่างจาก Google/Mapbox/Bing satellite ที่ต้องมี key) — error/fallback banner (`tileerror`/`tileload` listener) ผูกกับทั้งสอง layer ไม่ใช่แค่ layer เดียวเหมือนก่อน
- หน้ารายละเอียดแปลง (`view-plot-detail`) มีแผนที่ย่อ (`#plot-detail-map`, สูง 260px) ต่อจากรูปเอกสารที่ดิน แสดงขอบเขตแปลงที่บันทึกไว้เป็น `L.polygon` (เส้นต่อจุด 1→2→3→...→ปิดกลับจุด 1 เหมือนแผนที่หลัก) พร้อมหมุดต้นไม้ที่มีเลขลำดับของแปลงนั้น เป็น Leaflet map instance **แยกต่างหาก** จาก `map` หลัก (ตัวแปร `plotDetailMap`) — สร้าง/ทำลายใหม่ทุกครั้งที่เข้าหน้านี้ผ่าน `renderPlotDetailMap()` (เรียกจาก `setView('plot-detail')` เท่านั้น ไม่ได้เรียกจาก `renderAll()`/`renderPlotDetail()` เพราะตอนนั้น panel อาจยัง `display:none` อยู่ ทำให้ Leaflet วัดขนาด container ผิดเป็น 0×0) เป็น non-interactive preview (`dragging/scrollWheelZoom/...:false` ทั้งหมด) ไม่มี zoom control กันชนกับการ scroll หน้า ต้องเรียก `.remove()` ก่อนสร้างใหม่ทุกครั้งไม่งั้น Leaflet จะ error "Map container is already initialized"
- **(v1.11)** modal เพิ่ม/แก้ไขต้นไม้ เพิ่มฟิลด์และแก้ไขได้ครบ: **ลำดับที่** (`in-tree-seq`, แก้ไขได้อิสระ ไม่มี logic กันชน/สลับลำดับอัตโนมัติ ถ้าผู้ใช้ตั้งซ้ำกับต้นอื่นในแปลงเดียวกันระบบไม่เตือน — ผู้ใช้ต้องจัดการเอง), **โค้ดต้นไม้** (`code`, text field อิสระสำหรับเลขป้าย/tag), **รูปต้นไม้** (`photoUrl`, เปลี่ยนจาก text link เป็นปุ่มถ่ายรูป/อัปโหลดจริงเหมือนรูปแปลง — ไม่มีการประทับข้อมูล/ลายน้ำใดๆ), **รูปโค้ด/ป้ายต้นไม้** (`codePhoto` คอลัมน์ใหม่ในตาราง `trees`, ไว้ถ่ายรูปป้าย/QR ที่ติดกับต้นแยกจากรูปตัวต้นไม้) ทั้งสองรูป resize ผ่าน `resizeImageToDataUrl()` (maxDim 1000px, ไม่มี stamp/watermark ต่างจาก `stampPlotPhoto()`/`watermarkDocPhoto()` ของรูปแปลง) — รายการต้นไม้แต่ละต้นมีปุ่ม ✏️ เปิด `openEditTreeModal(tree)` (แก้ไขของเดิม) แยกจากปุ่ม ✕ ลบเดิม ฟังก์ชันเพิ่มใหม่ (คลิกแผนที่/ปุ่ม GPS) ใช้ `openAddTreeModal()` (เดิมชื่อ `openTreeModal()`) ตัวแปร state ร่วมกันทั้งสองโหมด: `editingTreeId`, `modalTreePhoto`, `modalCodePhoto` — **(อัปเดตภายหลัง)** ตำแหน่ง lat/lng แก้ไขได้แล้วด้วย ดูหัวข้อถัดไปเรื่อง "แก้ไขตำแหน่ง GPS"
- **(v1.12)** ต้องบันทึก "จุดอ้างอิง" ของแปลงก่อน 1 จุด จึงจะเริ่มปักหมุดต้นไม้ได้ — เก็บที่ระดับ**แปลง** ไม่ใช่ตาราง `trees` (columns `ref_lat`/`ref_lng`/`ref_description`/`ref_photos` ใหม่ในตาราง `plots`, object รูปแบบ `plot.refPoint = {lat, lng, description, photos:[]} | null`) จึงไม่กระทบเลขลำดับต้นไม้ ("ต้นที่ 1" ยังคงเป็นต้นไม้ต้นแรกจริงๆ ไม่ใช่จุดอ้างอิง) flow: ถ้า `plot.refPoint` ยังไม่มี คลิกแผนที่/กด GPS button (ในโหมดเพิ่ม/`addMode`) จะเปิด `openRefPointModal()` (ปุ่ม "บันทึกจุดอ้างอิง") แทน `openAddTreeModal()` — ข้อความ mode-toggle/GPS-button/crosshair banner (`updateBanner()`) เปลี่ยนไปตามสถานะนี้ด้วย (เช่น "โหมดบันทึกจุดอ้างอิง" ↔ "โหมดเพิ่มต้นไม้") ฟอร์มมีช่อง**คำอธิบาย** (text) และ**รูปถ่ายได้สูงสุด 4 รูป** (ถ่าย/อัปโหลด ปุ่มเดียวกับต้นไม้ resize ผ่าน `resizeImageToDataUrl()` ไม่มี stamp, กดปุ่มค้างจนครบ 4 รูปจะ disable ปุ่มถ่าย/อัปโหลดอัตโนมัติ) พอมี refPoint แล้วจะกลับสู่ flow ปกติถาวร จุดอ้างอิงแสดงเป็นหมุดธง 🚩 (`.ref-pin`) แยกจากหมุดต้นไม้เลขลำดับ ทั้งบนแผนที่หลักและแผนที่ย่อในหน้ารายละเอียดแปลง (แผนที่ย่อไม่มี popup) — บนหน้าต้นไม้ (`view-trees`) การ์ดจุดอ้างอิง (สไตล์เดียวกับ `.tree-item` แต่ใช้ 🚩 แทนเลขลำดับ, พื้นหลัง `.ref-point-item` ต่างสี) ขึ้น**ก่อน**รายการต้นไม้เสมอ มีปุ่ม ✏️ เปิด `openEditRefPointModal()` แก้ไขคำอธิบาย/รูป/ตำแหน่งได้ แยกจาก `openRefPointModal()` ที่ใช้ตอนยังไม่มี refPoint (มาจากคลิกแผนที่/GPS button, ตั้ง `pendingLatLng` ใหม่) ทั้งสองฟังก์ชันใช้ modal เดียวกัน (`#refpoint-modal-overlay`) สลับ title ผ่าน `editingRefPoint` flag + `#refpoint-modal-title` — **แผนที่ใหญ่หน้าหลัก (`view-home`) แสดงเฉพาะหมุดจุดอ้างอิงกับเส้นขอบเขตแปลง ไม่แสดงหมุดต้นไม้** (ตัวแปร `currentView` อัปเดตใน `setView()` แล้วเรียก `renderMarkers()` ทันที, `renderMarkers()` เช็ค `if(currentView !== 'home')` ก่อนวาดหมุดต้นไม้ — หมุดจุดอ้างอิงและเส้นขอบเขต (`renderBoundaries()`) ไม่ถูกกรอง แสดงทุกหน้าเหมือนเดิม) พอกดเข้าไปดูแปลงใดก็ตาม (`plot-detail`/`trees`) หมุดต้นไม้จะกลับมาแสดงตามปกติ
- **(v1.13) แก้ไขตำแหน่ง GPS ของต้นไม้/จุดอ้างอิงได้ 2 แบบ**: modal ทั้งสอง (เพิ่ม/แก้ไขต้นไม้ และเพิ่ม/แก้ไขจุดอ้างอิง) เปลี่ยนจาก `.gps-readout` แบบ read-only เป็นช่อง input เลข `in-tree-lat`/`in-tree-lng` (และ `in-refpoint-lat`/`in-refpoint-lng`) ที่พิมพ์แก้ตรงๆ ได้เลย บวกปุ่ม "📍 ใช้ตำแหน่ง GPS ปัจจุบัน" (เรียก `locateUser()` เดิม เติมค่าใส่ input ไม่ต้องปิด modal) และปุ่ม "🗺️ เลือกบนแผนที่" (`startPickingPosition('tree'|'refpoint')` — ซ่อน modal + ย่อแผง + ตั้ง state `pickingPositionFor`, รอคลิกแผนที่ครั้งถัดไปแล้ว `applyPickedPosition(latlng)` เติมพิกัดกลับเข้า input แล้วเปิด modal คืน) `map.on('click',...)` เช็ค `pickingPositionFor` เป็นเงื่อนไขแรกสุด (ก่อน `boundaryMode`/`addMode`) กันชนกับโหมดอื่น `setView()` reset `pickingPositionFor` เป็น safety net เผื่อผู้ใช้กดเมนูออกจากหน้าต้นไม้ระหว่างเลือกตำแหน่งค้างไว้ ปุ่ม "บันทึก" ทั้งสอง modal อ่านพิกัดจาก input โดยตรงเสมอ (ไม่ใช่จาก `pendingLatLng` อีกต่อไป ทั้ง flow เพิ่มใหม่และแก้ไข ใช้ path เดียวกันหมด) — ฟังก์ชันช่วย: `setLatLngInputs(prefix, lat, lng)` ใช้ร่วมกันทั้งสอง modal
- **(v1.14) ระบุตัวตนก่อนเพิ่มแปลงใหม่**: กดปุ่ม "+ เพิ่มแปลงใหม่" ครั้งแรกที่ยังไม่เคย login (เช็คผ่าน `GET /api/auth/me`) จะเจอ modal `#identify-modal-overlay` ("ระบุตัวตนก่อนเพิ่มแปลง") ให้กรอกเบอร์โทร+วันเดือนปีเกิด แล้วลอง login (`POST /api/auth/login`, `password` มาจาก `dobToPassword()` แปลงวันเกิดเป็นรูปแบบ `DDMMYYYY` 8 หลัก) ถ้า login ไม่ผ่าน (ยังไม่เคยลงทะเบียน หรือกรอกวันเกิดผิด) มีปุ่ม "ยังไม่เคยลงทะเบียน? กดที่นี่" เปิดฟอร์มเพิ่ม: ชื่อภาษาไทย, เลขบัตรประชาชน (13 หลัก, unique, validate ทั้ง client/server), แล้ว `POST /api/auth/register` ด้วย `{phone, password: dobToPassword(dob), name, nationalId, dob}` — สำเร็จแล้วได้ session cookie เดียวกับระบบ admin (`tm_session`, JWT อายุ 7 วัน) ทำให้ **"ใช้เครื่อง/เบราว์เซอร์เดิมไม่ต้อง login ซ้ำ"** ตามที่ผู้ใช้ต้องการได้แบบไม่ต้องเขียนอะไรเพิ่ม (cookie อยู่ยัน 7 วันเหมือน admin) — `users` table เพิ่ม column `name`, `national_id` (unique), `dob` (schema.sql `ALTER TABLE`) `server/db.js`/`test/memoryStore.js` เพิ่ม `findUserByNationalId` และ `createUser(...)` รับ arg ที่ 5 เป็น `extra={name,nationalId,dob}` (optional, ไม่ใส่ก็ยังใช้ได้เหมือนเดิม เข้ากันได้กับ `/api/admin/users` เดิมที่ไม่ส่ง extra) **ขอบเขตของการเช็คนี้แคบมาก**: ครอบคลุมแค่ปุ่ม "+ เพิ่มแปลงใหม่" ปุ่มเดียว ไม่ครอบคลุมการเพิ่มต้นไม้/แก้ไขแปลง-ต้นไม้เดิม/ดูข้อมูล (endpoint พวกนั้นยังเปิดสาธารณะเหมือนเดิมทั้งหมด) — โค้ดยังไม่ได้ผูกข้อมูลแปลงที่สร้างเข้ากับ user ที่ระบุตัวตน (ไม่มี column `created_by` ใน `plots`) ถ้าต้องการ track ว่าใครสร้างแปลงไหนต้องเพิ่มทีหลัง — **ข้อควรระวังเรื่องความปลอดภัย**: รหัสผ่าน=วันเกิด เดารหัสได้ง่ายถ้ารู้วันเกิดคนนั้น และเก็บเลขบัตรประชาชน (ข้อมูลอ่อนไหวตาม PDPA) เป็น plaintext ไม่ได้เข้ารหัส — ผู้ใช้ทราบ trade-off นี้แล้วและเลือกยอมรับเพื่อความง่ายในการใช้งานภาคสนาม
- **(v1.15) ข้อมูลเจ้าของแปลงล็อกตามผู้ใช้ที่ระบุตัวตนแล้ว แก้ไขไม่ได้**: หลังผ่าน flow ระบุตัวตน (v1.14, ไม่ว่าจะ login/register ใหม่ หรือมี session เดิมอยู่แล้ว) ช่อง "ชื่อเจ้าของแปลง"/"เบอร์ติดต่อ" ในฟอร์มเพิ่มแปลงใหม่จะถูกเติมชื่อ+เบอร์โทรของผู้ใช้ที่ระบุตัวตนโดยอัตโนมัติและเป็น **`readonly` พิมพ์แก้เองไม่ได้เด็ดขาด** (ตั้งใจ — ข้อมูลแปลงที่สร้างต้องเป็นของ user ที่ระบุตัวตนเท่านั้น ห้ามกรอกแทนคนอื่น) `prefillOwnerFromIdentity()` set ค่าทับทุกครั้งที่เข้าฟอร์ม (ไม่เช็คว่าช่องว่างอยู่ไหมเหมือนตอนแรกที่ทำผิด เพราะ field เป็น readonly ผู้ใช้แก้เองไม่ได้อยู่แล้วจึงไม่มีค่าที่ต้องรักษาไว้) เรียกซ้ำหลังบันทึกแปลงสำเร็จด้วย (แทนที่การ reset เป็นค่าว่างแบบเดิม) เพื่อให้แปลงถัดไปในเซสชันเดียวกันยังคงมีข้อมูลเจ้าของถูกต้อง — ตัวแปร global `identifiedUser` เก็บ user object ที่ได้จาก `/api/auth/me` (ตอนเข้าฟอร์มโดยมี session อยู่แล้ว ผ่าน `checkFieldSession()` ที่แก้ให้ return user object แทน boolean) หรือจาก response ของ `/api/auth/login`/`/api/auth/register` ตรงๆ (ตอน identify ใหม่) — **ต้องเพิ่ม `name` เข้าไปใน JWT payload ด้วย** (`server/auth.js` `signToken()`) เพราะเดิม JWT มีแค่ `sub/email/phone/role/managedCommunityEnterpriseId` ไม่มีชื่อ ทำให้ `GET /api/auth/me` (อ่านจาก `req.user` ที่ decode จาก JWT) ไม่มี `name` ให้ส่งกลับ — ผลข้างเคียง: บัญชีที่ login ค้างไว้ตั้งแต่ก่อนแก้ (JWT เดิมไม่มี `name`) จะไม่เห็นชื่อจนกว่าจะ logout/login ใหม่ครั้งหนึ่ง (เหมือน trade-off เรื่อง role staleness ที่มีอยู่แล้ว) — `POST /api/auth/login` response ก็เพิ่ม `name` เข้าไปด้วยเช่นกัน (เดิมส่งแค่ `id/email/phone/role/managedCommunityEnterpriseId`) — **ขอบเขต**: ล็อกแค่ตอนสร้างแปลงใหม่ผ่านฟอร์มนี้เท่านั้น ไม่ได้ป้องกันการแก้ไข ownerName/ownerContact ของแปลงที่มีอยู่แล้วผ่าน `PUT /api/plots/:id` โดยตรง (endpoint นั้นยังเปิดสาธารณะไม่มี auth เหมือนเดิมทั้งหมด ตามข้อจำกัดเดิมที่ห้ามแตะ)
- **(v1.16) เมนูเข้าสู่ระบบ/ออกจากระบบในแอปภาคสนาม**: เพิ่มปุ่มที่ 3 ในเมนู ☰ (`#menu-item-auth`) ต่อจาก "ย่อ/ขยายแผงข้อมูล" — สลับข้อความ/พฤติกรรมตามสถานะ session: ยังไม่ระบุตัวตน → "👤 เข้าสู่ระบบ" กดแล้วเปิด modal ระบุตัวตนเดิม (v1.14), ระบุตัวตนแล้ว → "🚪 ออกจากระบบ (ชื่อผู้ใช้)" กดแล้ว confirm ก่อนแล้วค่อยเรียก `POST /api/auth/logout` label อัปเดตผ่าน `updateAuthMenuLabel()`/`refreshAuthMenuState()` (เรียกตอนเปิดเมนูทุกครั้งเพื่อความสด + ตอน boot แอปครั้งแรกด้วย เผื่อ session ยังไม่หมดอายุจากการเข้าครั้งก่อน) — **ต้องแยก "จุดประสงค์" ของการเปิด modal ระบุตัวตน 2 แบบ** เพราะเดิม modal นี้ถูกออกแบบมาเฉพาะ flow "+ เพิ่มแปลงใหม่" ที่ login/register สำเร็จแล้วจะ `setView('plot-form')` ทันที แต่ถ้าเปิดจากเมนูโดยตรง (แค่อยากล็อกอิน/สลับบัญชี ไม่ได้ตั้งใจจะเพิ่มแปลง) ไม่ควรถูกเด้งไปหน้าเพิ่มแปลงให้ — เพิ่มตัวแปร `identifyModalPurpose` ('add-plot' | 'menu') ที่ `openIdentifyModal(purpose)` รับเป็น argument, เช็คค่านี้ก่อนตัดสินใจ `setView('plot-form')` ในทั้ง login/register success handler เท่านั้น (เปิดจากเมนูจะปิด modal เฉยๆ อยู่หน้าเดิมต่อ)
- **(v1.17) subtitle ใต้หัวข้อ sidebar แสดงชื่อหน้าที่กำลังทำงานอยู่แทนคำโปรยคงที่**: เดิม `#sidebar-header p` เป็นข้อความคงที่ "บันทึกแปลง เจ้าของแปลง และตำแหน่งต้นไม้ · เวอร์ชัน vX.X" เปลี่ยนเป็นแสดงชื่อ view ปัจจุบันแทน (เพื่อให้อ้างอิงหน้าที่กำลังใช้งานอยู่ได้ง่ายเวลาคุย/แจ้งปัญหา) ยังคงเลขเวอร์ชันต่อท้ายไว้เหมือนเดิม รูปแบบ "ชื่อหน้า · เวอร์ชัน vX.X" — เพิ่ม `<span id="sidebar-subtitle-view">` แยกจาก span เวอร์ชัน, ตาราง `VIEW_TITLES` แม็ป view name → ข้อความไทย (`home`→"หน้าแรก · รายการแปลงที่ดิน", `plot-form`→"เพิ่มแปลงใหม่", `plot-detail`→"รายละเอียดแปลง", `trees`→"เพิ่ม/จัดการต้นไม้ในแปลง") อัปเดตค่าใน `setView(view)` ทุกครั้งที่สลับหน้า (ไม่ต้องเรียกแยกตอน boot เพราะ HTML ตั้งค่าเริ่มต้นเป็น "หน้าแรก..." ตรงกับ `currentView` default อยู่แล้ว)
- **(v1.18) แก้ไขรายละเอียดแปลงได้จากหน้า "รายละเอียดแปลง" + เพิ่มฟิลด์ตำบล**: ปุ่มดินสอ✏️ (`#btn-edit-plot-detail`) มุมขวาบนของ banner ข้อมูลแปลงใน `view-plot-detail` เปิดโหมดแก้ไขแบบ inline แทนที่ banner แสดงผลปกติ (`editingPlotDetail` flag คุมสลับระหว่าง `renderPlotDetailInfo()` ปกติกับ `renderPlotDetailEditForm()`) แก้ไขได้: ชื่อแปลง, เอกสารสิทธิ์, เนื้อที่ (ไร่/งาน/ตร.วา), ที่ตั้ง (ตำบล/อำเภอ/จังหวัด/รหัสไปรษณีย์) — **ไม่รวมขอบเขตแปลง** (boundary ไม่เปลี่ยน ต้องลบสร้างใหม่ถ้าจะแก้ขอบเขต) — เหมือน "+ เพิ่มแปลงใหม่" (v1.14/v1.15) กดปุ่มแก้ไขต้องระบุตัวตนก่อนถ้ายังไม่เคย (เปิด `openIdentifyModal('edit-plot')` เพิ่ม purpose ที่ 3 ต่อจาก `'add-plot'`/`'menu'` — login/register สำเร็จจากโหมดนี้จะเข้าโหมดแก้ไขทันทีโดยไม่เปลี่ยนหน้า ต่างจาก `'add-plot'` ที่ setView ไป plot-form) ช่อง **"ชื่อเจ้าของแปลง"/"เบอร์ติดต่อ" เป็น readonly บังคับเป็นข้อมูลของผู้ใช้ที่ระบุตัวตนอยู่ ณ ขณะนั้นเสมอ** (เขียนทับ ownerName/ownerContact เดิมของแปลงด้วยข้อมูล user ปัจจุบันตอนกด "บันทึก" ทุกครั้ง — เจตนาเดียวกับ v1.15 คือห้ามกรอกข้อมูลเจ้าของแทนคนอื่น แม้จะเป็นการแก้ไขแปลงที่มีอยู่แล้วก็ตาม) ปุ่ม "ยกเลิก" กลับไป view ปกติโดยไม่บันทึก, `setView()` reset `editingPlotDetail=false` เป็น safety net ทุกครั้งที่เปลี่ยนหน้า (กันเข้าไปดูแปลงอื่นแล้วยังค้างฟอร์มแก้ไขของแปลงเดิม) — เพิ่มคอลัมน์/ฟิลด์ใหม่ `subdistrict` (ตำบล/แขวง) ทั้งในฟอร์มเพิ่มแปลงใหม่และฟอร์มแก้ไขนี้ (ไม่ใช่แค่ฟอร์มแก้ไขอย่างเดียว เพื่อไม่ให้แปลงที่สร้างใหม่ขาดข้อมูลนี้) — `loadThaiZipIndex()`/`handlePostcodeInput()` ปรับใหม่ทั้งคู่: เดิม dedup ตาม (อำเภอ,จังหวัด) ทิ้งข้อมูลตำบลไป ตอนนี้เก็บระดับตำบลด้วย (`sub_district.json`'s `name_th` เป็นชื่อล้วนไม่มีคำนำหน้า ต่างจาก `district.json` ที่มีคำนำหน้า "เขต"/"อำเภอ" ติดมาด้วย — ต้องเดาคำนำหน้าตำบลเอาเองจากบริบทอำเภอ: ถ้าอำเภอขึ้นต้น "เขต" (กรุงเทพฯ) ตำบลใช้ "แขวง" มิฉะนั้นใช้ "ตำบล" ธรรมดา ผิดพลาดมาก่อน 1 รอบระหว่าง dev — ทดสอบแล้วว่าตอนนี้ถูกต้องทั้งกรุงเทพฯ และต่างจังหวัด) ฟังก์ชัน `handlePostcodeInput`/`handlePostcodeInputFor(ids)` รับ object ของ element id เป็น argument เพื่อใช้ซ้ำได้ทั้งฟอร์มเพิ่มแปลงใหม่ (`ADD_PLOT_ADDR_IDS`) และฟอร์มแก้ไข (`EDIT_PLOT_ADDR_IDS`) — 1 รหัสไปรษณีย์อาจ map กับหลายตำบล/อำเภอ (พบได้บ่อยมาก โดยเฉพาะกรุงเทพฯ) ต้องเปิด dropdown ให้เลือกเหมือนเดิม (ตอนนี้ label แสดงตำบลด้วยไม่ใช่แค่อำเภอ/จังหวัด) — `addressText()` เพิ่มบรรทัดตำบลนำหน้าอำเภอ (ยังคง hardcode คำนำหน้า "ตำบล" เสมอในจุดแสดงผลสรุปนี้ ไม่ได้แยกกรุงเทพฯ/ต่างจังหวัดเหมือนใน dropdown เลือกที่อยู่ เป็นความไม่ตรงกันเล็กน้อยที่มีมาจากเดิมอยู่แล้วสำหรับ district ก็เขียน "อำเภอ" เสมอไม่แยก "เขต" เหมือนกัน ไม่ได้แก้เพิ่มเพราะสอดคล้องกับพฤติกรรมเดิมของแอป) — **บั๊กที่พบและแก้ในรอบถัดมา (v1.19)**: ตอนเพิ่ม `subdistrict` ใน v1.18 แก้แค่ `index.html` (client) แต่ลืมเพิ่มคอลัมน์ใน `schema.sql`/`server/db.js` (`plotRowToObj`, `upsertPlot`) จริง — ทดสอบตอนนั้นผ่านหมดเพราะ `npm run dev`/`npm test` ใช้ in-memory store (`test/memoryStore.js`) ที่ไม่มี fixed schema (spread object ผ่านได้หมดไม่ว่าจะมี field อะไร) ทำให้ bug ไม่ถูกจับได้เลยจนกว่าจะรันกับ Postgres จริง ค่าตำบลที่กรอกจะถูก "กลืนหาย" เงียบๆ ตอน deploy ขึ้น Render จริง (Postgres จะ insert แค่คอลัมน์ที่ query ระบุไว้ ฟิลด์ที่ไม่รู้จักถูก ignore เฉยๆ ไม่ error) — **บทเรียน**: ทุกครั้งที่เพิ่ม field ใหม่ในตาราง `plots`/`trees`/`users` ต้องแก้ 3 จุดพร้อมกันเสมอ: (1) `schema.sql` ALTER TABLE, (2) `server/db.js` (`xRowToObj` + `upsertX` ทั้ง column list และ VALUES), (3) `test/memoryStore.js` ถ้า field นั้นมี logic พิเศษ (เช่น immutable) — แค่แก้ client ฝั่งเดียวไม่พอ และ `npm test` ที่ผ่านไม่ได้แปลว่า schema จริงถูกต้องเสมอไปเพราะ in-memory store ไม่บังคับ schema
- **(v1.19) ผูกข้อมูลแปลงกับ user จริงผ่าน `created_by`**: เพิ่มคอลัมน์ `plots.created_by` (FK ไป `users.id`, `ON DELETE SET NULL` เหมือน `community_enterprise_id`) — ก่อนหน้านี้ (v1.14-v1.18) การ "ผูก" มีแค่ระดับ copy ค่า `ownerName`/`ownerContact` เป็น text ธรรมดา ไม่มี relational link จริงเลย ตอนนี้มีคอลัมน์อ้างอิงจริงแล้ว **`created_by` เป็น immutable หลังสร้างครั้งแรก** — index.html ส่ง `createdBy: identifiedUser.id` แนบไปทุกครั้งทั้งตอนสร้างและตอนแก้ไขแปลง แต่ server (`server/db.js` `upsertPlot`) จงใจไม่ใส่ `created_by` ไว้ใน `ON CONFLICT DO UPDATE SET` clause ทำให้ Postgres คงค่าเดิมไว้เสมอตอน update ไม่สนใจค่าที่ส่งมาใหม่ (กันแก้ไข "เจ้าของประวัติศาสตร์" ของแปลงแม้จะมีคนอื่นมาแก้ไขแปลงทีหลัง) — **สำคัญ**: endpoint `PUT /api/plots/:id` ยังคงเปิดสาธารณะไม่มี auth เหมือนเดิม ดังนั้น `createdBy` เป็นค่าที่ client-asserted (ผู้ใช้ทั่วไปที่ทำ API call ตรงๆ ปลอมค่านี้ได้ ไม่ได้ validate จาก session จริง) เทียบเท่าระดับความน่าเชื่อถือเดียวกับ `ownerName`/`ownerContact` ที่มีมาก่อนแล้ว (ล็อกแค่ระดับ UI ผ่านฟอร์มเท่านั้น) — ยังไม่มี UI แสดงผล "สร้างโดยใคร" ในหน้าไหนเลย (ไม่ใช่ scope ของงานนี้ ทำแค่ผูกข้อมูล/relational link ให้พร้อมสำหรับใช้งานทีหลัง เช่น query "แปลงทั้งหมดที่ user คนนี้สร้าง") ทดสอบ immutability ครบทั้ง in-memory store (`test/api.test.js`) และ Postgres จริง (สร้าง database ทดสอบชั่วคราว `treemap_test`, ยืนยันว่า schema migrate ถูกต้อง, `created_by` คงค่าเดิมตอน edit แม้ client จะส่งค่าอื่นมา, และ `ON DELETE SET NULL` ทำงานถูกต้องเมื่อลบ user เจ้าของออก — ไม่ cascade ลบแปลงตามไปด้วย — ลบ database ทดสอบทิ้งหลังยืนยันเสร็จ)
- **(v1.20) หน้าแรกของแอปภาคสนามแสดงเฉพาะแปลงของ user ที่ระบุตัวตนอยู่เท่านั้น**: ใช้ `plots.created_by` (v1.19) กรองการแสดงผลฝั่ง client — ฟังก์ชัน `isPlotVisibleToUser(plot)`/`visiblePlots()` เป็นตัวกลางเดียวที่ใช้กรองทั้ง `renderPlotList()` (รายการแปลงหน้าแรก), `renderMarkers()` (หมุดจุดอ้างอิง + หมุดต้นไม้), และ `renderBoundaries()` (เส้นขอบเขตแปลงบนแผนที่ใหญ่) — เกณฑ์การมองเห็น: แปลงที่ `created_by` ตรงกับ `identifiedUser.id`, **หรือ** แปลงเก่าที่ไม่มี `created_by` เลย (สร้างไว้ตั้งแต่ก่อนมีระบบนี้ v1.14-v1.18 ทั้งหมด) จะแสดงให้ **ทุกคน** เห็นเหมือนเดิม (ตามที่ผู้ใช้เลือกไว้ — ยังไม่มีคนมาผูกเจ้าของให้) — **ถ้ายังไม่เคยระบุตัวตนเลย หน้าแรกจะว่างเปล่าเสมอ พร้อมข้อความ + ปุ่ม "เข้าสู่ระบบ/ลงทะเบียน" ให้กดได้ทันที** (`openIdentifyModal('home')` — purpose ที่ 4 ต่อจาก `'add-plot'`/`'edit-plot'`/`'menu'`, พฤติกรรมหลัง login/register สำเร็จเหมือน `'menu'` คือปิด modal เฉยๆ ไม่เปลี่ยนหน้า เพราะ logic เดิมเป็น if/else-if แบบไม่มี else จับ purpose อื่นๆ) — **จุดที่ต้องระวัง**: การเปลี่ยนสถานะ identity (login/register/logout ผ่านช่องทางไหนก็ตาม) ต้องเรียก `renderAll()` ทั้งฟังก์ชันเสมอ ไม่ใช่แค่ `renderPlotList()` — พลาดไป 1 รอบระหว่าง dev (เรียกแค่ `renderPlotList()` ทำให้ list อัปเดตถูกแต่เส้นขอบเขตบนแผนที่ใหญ่ยังค้างของเก่า/ว่างเปล่าอยู่) แก้แล้วให้เรียก `renderAll()` ที่ login/register success handler ทั้งสอง (`btn-identify-login`/`btn-identify-register`) และ logout handler (`menu-item-auth` เมื่อ `identifiedUser` มีค่าอยู่) — ส่วน `openAddPlotFlow()` (กรณีมี session อยู่แล้วไม่ต้องเปิด modal) เพิ่ม `renderBoundaries()` แยกก่อน `setView('plot-form')` เพราะ `setView()` เรียกแค่ `renderMarkers()` ให้อัตโนมัติ ไม่เรียก `renderBoundaries()` — ตอน boot แอป เปลี่ยนลำดับจาก `renderAll()` แล้วค่อย `refreshAuthMenuState()` (fire-and-forget) เป็น **`await refreshAuthMenuState()` ก่อน แล้วค่อย `renderAll()`** เพื่อกันไม่ให้ผู้ใช้ที่มี session ค้างอยู่แล้ว (cookie 7 วัน) เห็นหน้าแรกว่างเปล่ากะพริบก่อนแล้วค่อยโผล่แปลงของตัวเองขึ้นมาทีหลัง (เพิ่ม delay การ paint ครั้งแรกเล็กน้อยแลกกับไม่มีอาการกะพริบ) — ทดสอบผ่านเบราว์เซอร์จริงครบ: user คนละคนเห็นแปลงคนละชุด (ของตัวเอง+แปลงเก่าไร้เจ้าของ ไม่เห็นของอีกฝ่าย) ทั้ง list และเส้นขอบเขตบนแผนที่, ยังไม่ระบุตัวตนเห็นหน้าว่างพร้อมปุ่ม, login/logout อัปเดตทุกอย่างถูกต้องโดยไม่ต้อง refresh หน้าเอง
- **(v1.21) ช่องวันเกิดใน modal ระบุตัวตน เปลี่ยนจาก `<input type="date">` เป็นพิมพ์ตัวเลข 8 หลัก ปีเป็น พ.ศ.**: `#in-identify-dob` เดิมเป็น native date picker (ปฏิทิน ค.ศ. ซึ่งคนไทยภาคสนามไม่คุ้น) เปลี่ยนเป็น `type="text" inputmode="numeric" maxlength="8"` ให้พิมพ์ตรงๆ รูปแบบ "วว ดด ปปปป" โดย ปปปป เป็นปี **พ.ศ.** (เช่น เกิด 15 มี.ค. 2533 พิมพ์ "15032533") — ฟังก์ชันใหม่ `parseThaiDob(digits)` validate รูปแบบ (ต้องเป็นตัวเลข 8 หลักพอดี, วันที่ 1-31, เดือน 1-12, แปลง พ.ศ.→ค.ศ. ด้วยการลบ 543 แล้ว sanity check ว่าอยู่ในช่วง ค.ศ. 1900-2100) คืนค่า `{password, iso}` หรือ `null` ถ้ารูปแบบผิด — **สำคัญเรื่อง backward compatibility**: `password` ที่คำนวณได้ยังคงเป็น `DD+MM+ค.ศ.YYYY` เหมือนของเดิมทุกประการ (แค่แปลงปีจาก พ.ศ. ที่ผู้ใช้พิมพ์กลับเป็น ค.ศ. ก่อนต่อ string) ทำให้ผู้ใช้ที่ลงทะเบียนไว้ก่อนหน้านี้ (ตอนยังเป็น date picker ค.ศ.) ยัง login ด้วยวันเกิดเดิมของตัวเองได้ปกติ ไม่ต้องลงทะเบียนใหม่ — ฟิลด์ `dob` ที่ส่งไป `POST /api/auth/register` เปลี่ยนจากส่งค่า input ดิบ เป็นส่ง `parsedDob.iso` (ISO ค.ศ. ที่แปลงแล้ว) เพื่อให้ backend เก็บ `dob` เป็น Gregorian date เหมือนเดิม ไม่กระทบ schema/backend เลย แก้แค่ฝั่ง client — ทดสอบผ่านเบราว์เซอร์จริงครบ: ลงทะเบียนด้วยวันเกิด พ.ศ. → login ซ้ำด้วยวันเกิด พ.ศ. เดิมสำเร็จ (ยืนยัน round-trip), กรอกรูปแบบผิด (เช่น เดือน 13, ตัวอักษรปน, จำนวนหลักไม่ครบ) ขึ้น error ข้อความบอกรูปแบบที่ถูกต้อง
- **(v1.22) ซูมแผนที่ไปตำแหน่ง GPS ปัจจุบันอัตโนมัติ เมื่อ login/register สำเร็จ หรือตอนโหลดหน้าแรก**: `locateUser(onDone, silent)` เพิ่ม parameter `silent` (ถ้า `true` จะไม่ `alert()` เตือนตอนหา GPS ไม่สำเร็จ/เบราว์เซอร์ไม่รองรับ — ใช้กับการเรียกอัตโนมัติที่ผู้ใช้ไม่ได้กดปุ่มเอง ต่างจากปุ่ม 📍 GPS เดิมที่ยัง alert ตามปกติ) เรียก `locateUser(null, true)` อัตโนมัติ 2 จุด: (1) ใน `main()` ตอน boot แอป หลัง `renderAll()` ถ้า `currentView === 'home'` (ครอบคลุมทุกครั้งที่เปิด/รีโหลดหน้าเว็บ เพราะ view เริ่มต้นเป็น home เสมอ) (2) ใน login/register success handler ของ modal ระบุตัวตน (shared code block ที่ใช้ทั้งสองปุ่ม "เข้าสู่ระบบ"/"ลงทะเบียน" — ครอบคลุมทุก purpose ของ modal ทั้ง `add-plot`/`edit-plot`/`menu`/`home` เพราะ "login สำเร็จ" ใช้ความหมายเดียวกันไม่ว่าจะเปิด modal มาจากทางไหน) **ไม่ได้เพิ่มที่ fast-path ที่ session มีอยู่แล้ว** (`openAddPlotFlow()`/`startEditPlotDetail()` ตอนไม่ต้องเปิด modal เพราะ login ค้างอยู่แล้ว) เพราะนั่นไม่ใช่ "login สำเร็จ" ใหม่ แค่ใช้ session เดิมต่อ — ทดสอบผ่านเบราว์เซอร์จริงด้วยการ mock `navigator.geolocation.getCurrentPosition` (เพราะ headless/automated browser ไม่มีตำแหน่ง GPS จริง) ยืนยันว่า marker ย้ายไปตำแหน่งที่ mock ไว้ถูกต้องทั้ง 2 จุดเรียก — **หมายเหตุระหว่างทดสอบ**: `map.getCenter()`/`map.getZoom()` ที่เรียกผ่าน JS ทันทีหลัง `flyTo()` คืนค่าเก่าไม่อัปเดตในบราว์เซอร์ที่ควบคุมด้วย automation (แต่ marker และภาพหน้าจอจริงถูกต้อง) เข้าใจว่าเป็นเพราะ `requestAnimationFrame` ที่ Leaflet's flyTo ใช้ถูก throttle ในแท็บที่ควบคุมด้วย CDP ไม่ใช่บั๊กของแอป — ถ้าจะเขียนเทสต์อัตโนมัติสำหรับพฤติกรรมนี้อีกในอนาคต ให้ตรวจสอบผ่านภาพหน้าจอ (screenshot) หรือตำแหน่ง marker แทนการอ่านค่า `getCenter()`/`getZoom()` ตรงๆ
- **(v1.23) ระบบสถานะแปลง (workflow ป้อนข้อมูล → สำรวจ → ส่งตรวจสอบ → ผ่าน/ไม่ผ่าน)**: `plots.status` (`data_entry` | `tree_survey` | `submitted` | `approved`, default `'data_entry'`) + `plots.review_note`/`plots.review_photos` (เหตุผล+รูปประกอบการเปลี่ยนสถานะครั้งล่าสุด เก็บค่าเดียวไม่ใช่ history เต็มรูปแบบ) — **การเปลี่ยนสถานะแยกออกจาก endpoint แก้ไขข้อมูลทั่วไปโดยเจตนา**: `PUT /api/plots/:id` (ฟอร์มแก้ไขข้อมูลแปลง/จุดอ้างอิง) และ `PUT /api/trees/:id` (เพิ่ม/แก้ไขต้นไม้) **ไม่แตะ `status`/`review_note`/`review_photos` เลย** (คอลัมน์พวกนี้ไม่อยู่ใน `ON CONFLICT DO UPDATE SET` ของ `upsertPlot` เหมือน `created_by` — immutable ผ่าน route นี้ ตั้งได้แค่ตอน insert แรกเท่านั้น) การเปลี่ยนสถานะทำได้แค่ 2 ทาง: (1) `POST /api/plots/:id/submit` (public, ไม่ต้อง login — ปุ่ม "ส่งแปลงตรวจสอบ" ของ field worker เอง) เปลี่ยน `tree_survey`→`submitted` เท่านั้น 409 ถ้าสถานะไม่ใช่ `tree_survey`, (2) `PATCH /api/admin/plots/:id/status` (`requireAdminOrEnterpriseAdmin`, เปลี่ยนไปสถานะไหนก็ได้ใน 4 ค่า) **บังคับส่ง `note` ที่ไม่ว่างเสมอ** (400 ถ้าไม่ใส่) ตามที่ผู้ใช้กำหนด "ต้องใส่เหตุผลประกอบด้วย" — enterprise_admin แก้ได้เฉพาะแปลงในกลุ่มตัวเอง (helper `canManagePlot()` เทียบ `plot.communityEnterpriseId` กับ `req.user.managedCommunityEnterpriseId`, admin เต็มแก้ได้ทุกแปลง) — **auto-transition `data_entry`→`tree_survey`**: อยู่ใน `PUT /api/trees/:id` ฝั่ง server (ไม่ใช่ client) เช็คว่าเป็นต้นไม้ต้นใหม่ (`findTreeById` หาไม่เจอมาก่อน) และ plot ยังเป็น `data_entry` อยู่ ถ้าใช่เรียก `bumpPlotToTreeSurvey()` (atomic `UPDATE ... WHERE status='data_entry'`) — เพิ่มต้นไม้ต้นถัดๆ ไปไม่ทำอะไรเพิ่มเพราะสถานะไม่ใช่ `data_entry` แล้ว — **การล็อกแก้ไข**: เมื่อ `status` เป็น `submitted`/`approved` (`LOCKED_PLOT_STATUSES`) ทั้ง `PUT /api/plots/:id` และ `PUT /api/trees/:id` (ทั้งสร้างใหม่และแก้ไข) คืน `409` — บังคับที่ระดับ server จริง ไม่ใช่แค่ซ่อนปุ่มฝั่ง client (ต่างจาก `ownerName` lock ของ v1.15 ที่เป็น client-only เพราะ integrity requirement ของฟีเจอร์นี้จริงจังกว่า ถ้าปล่อยให้ bypass ผ่าน API ตรงได้จะทำลายความหมายทั้งหมดของการ "ตรวจสอบผ่านแล้ว") **DELETE ไม่ถูกบล็อกที่ server** (ลบแปลง/ต้นไม้ที่ล็อกอยู่ยังทำได้ผ่าน API ตรง — ตั้งใจจำกัดขอบเขตแค่ตามที่ระบุไว้ชัดเจน 4 อย่าง: แก้ข้อมูลแปลง/จุดอ้างอิง/ข้อมูลต้นไม้/เพิ่มต้นไม้ ไม่รวมการลบ) แต่ซ่อนปุ่มลบไว้ฝั่ง client เมื่อ log — **ฝั่งแอปภาคสนาม**: badge สีตามสถานะบนการ์ดแปลงหน้าแรกและหน้ารายละเอียด (`plotStatusBadge()`, `PLOT_STATUS_LABELS`/`PLOT_STATUS_COLORS`), กล่องเตือนสีแดงอ่อนแสดงเหตุผล+รูปตอนตรวจสอบไม่ผ่าน (เงื่อนไขแสดงคือ `status==='tree_survey' && plot.reviewNote` เท่านั้น — ถ้าสถานะเปลี่ยนไปแล้ว เช่นส่งใหม่/ผ่านแล้ว จะไม่โชว์ note เก่าซ้ำแม้ field ในข้อมูลจะยังไม่ถูกล้างก็ตาม), ปุ่ม "📤 ส่งแปลงตรวจสอบ" โชว์เฉพาะสถานะ `tree_survey` (disabled ถ้ายังไม่มีต้นไม้เลย), ซ่อนปุ่มแก้ไขข้อมูลแปลง/ปุ่มแก้ไขจุดอ้างอิง/ปุ่มแก้ไข-ลบต้นไม้/โหมดเพิ่มต้นไม้/ปุ่มถ่ายรูปแปลง-เอกสารทั้งหมดเมื่อ `isPlotLocked(plot)` — **ฝั่งเว็บ admin**: คอลัมน์ "สถานะ" แบบ badge สีใน `PlotsTable.jsx`, ปุ่ม "จัดการสถานะ" เปิด `PlotStatusModal.jsx` (เลือกสถานะเป้าหมาย + เหตุผลบังคับกรอก + แนบรูปได้สูงสุด 4 รูปผ่าน `FileReader`/dataURL เหมือน pattern เดิมของ ref point) ปุ่ม "แก้ไข" ในตารางถูก `disabled` เมื่อแปลงล็อกอยู่ (ฝั่ง client mirror กติกาเดียวกับ server) — ทดสอบ backend ครบใน `test/plotStatus.test.js` (7 เคส: default status, auto-transition, submit gate, lock enforcement บน plot/tree ทั้งสร้าง/แก้, validation ของ endpoint เปลี่ยนสถานะ, reject flow คืนค่า note/photos ถูกต้องและปลดล็อกจริง, enterprise_admin scope) ยืนยันกับ Postgres จริงด้วย (สร้าง database ทดสอบชั่วคราว ตรวจ schema migrate ถูกต้อง, round-trip ทุกฟังก์ชัน store ใหม่, ยืนยันว่า plain `upsertPlot` ไม่ทำให้ status/reviewNote หายหรือเปลี่ยนโดยไม่ตั้งใจ) ทดสอบ end-to-end ผ่านเบราว์เซอร์จริงครบวงจร: สร้างแปลง→เพิ่มต้นไม้(status auto-bump)→ส่งตรวจสอบ→ล็อกถูกต้องทั้ง client และ server (ลองยิง API ตรงยืนยันโดน 409)→admin ตีกลับพร้อมเหตุผล+รูป→ปลดล็อก แก้ไขสำเร็จ→ส่งใหม่→admin อนุมัติ→ล็อกอีกครั้งพร้อมข้อความ "ตรวจสอบผ่านแล้ว" ถูกต้องทุกขั้นตอน
- **(v1.24) ปุ่ม "ส่งแปลงตรวจสอบ" ย้ายไปอยู่ล่างสุดของหน้ารายละเอียดแปลง**: เดิม (v1.23) ปุ่มนี้อยู่ในบล็อกเดียวกับ banner ข้อมูลแปลง (ใกล้ด้านบนของหน้า) ย้ายมาไว้เป็นส่วนแยกต่างหาก `#plot-submit-area` ต่อจากแผนที่ย่อ (`#plot-detail-map`) ที่ท้ายสุดของ `view-plot-detail` แทน — สไตล์ใหม่ `.btn-submit-plot` (สีส้ม `#E8720C`, ใหญ่กว่าปุ่มทั่วไป padding 18px/font-size 17px/สูงขั้นต่ำ 58px, มี box-shadow) ต่างจาก `.btn` มาตรฐานของแอปที่เป็นสีเขียว `--primary` ปกติ — ฟังก์ชันแยกใหม่ `renderPlotSubmitArea()` เรนเดอร์ปุ่ม/ข้อความสถานะ (⏳ รอตรวจสอบ, ✅ ผ่านแล้ว) ลงใน container นี้โดยเฉพาะ เรียกจากท้าย `renderPlotDetailInfo()` ทุก branch (รวม branch `editingPlotDetail`/ไม่มีแปลงเลือก ที่ต้องเคลียร์พื้นที่นี้ด้วยไม่ให้ปุ่มเก่าค้าง) — ข้อความ/เงื่อนไขการแสดงผล (disabled ถ้ายังไม่มีต้นไม้, ซ่อนถ้ากำลังแก้ไขข้อมูลแปลงอยู่) เหมือนเดิมทุกประการ เปลี่ยนแค่ตำแหน่งและสไตล์
- **(v1.25) บั๊ก: ปุ่ม "ส่งแปลงตรวจสอบ" ดูเหมือนหายไปสำหรับแปลงเก่า**: ผู้ใช้แจ้งว่า "ไม่เห็นปุ่ม" — สาเหตุคือ 2 เรื่องรวมกัน (1) ปุ่มถูกออกแบบให้ซ่อนเลยเมื่อสถานะยังเป็น `data_entry` (ยังไม่เคยเพิ่มต้นไม้) ทำให้ดูเหมือน "ไม่มีปุ่มนี้อยู่" ทั้งที่จริงๆ คือรอเงื่อนไข ไม่ใช่บั๊ก, (2) **บั๊กจริง**: แปลงที่สร้างไว้ก่อน v1.23 (ตอนยังไม่มีระบบสถานะ) จะได้ `status='data_entry'` จาก column default ตอน migrate แม้จะมีต้นไม้อยู่แล้วก็ตาม เพราะ auto-transition `data_entry`→`tree_survey` ทำงานเฉพาะตอน "เพิ่มต้นไม้ต้นใหม่" เท่านั้น (เช็คใน `PUT /api/trees/:id`) ต้นไม้ที่มีอยู่ก่อนหน้าไม่ทำให้ transition นี้ทำงานย้อนหลัง แปลงเก่าจึงติดอยู่ที่ `data_entry` ตลอดไปจนกว่าจะมีคนเพิ่มต้นไม้ใหม่อีกต้น — **แก้ 2 จุด**: (1) เพิ่ม backfill migration ท้าย `schema.sql` (`UPDATE plots SET status='tree_survey' WHERE status='data_entry' AND EXISTS (SELECT 1 FROM trees WHERE trees.plot_id=plots.id)`) รันซ้ำได้ปลอดภัยทุก boot เพราะแตะเฉพาะแถวที่ยังเป็นค่า default อยู่เท่านั้น (ทดสอบกับ Postgres จริงแล้วว่า: แปลงเก่ามีต้นไม้ถูก bump ถูกต้อง, แปลงยังไม่มีต้นไม้ไม่ถูกแตะ, แปลงที่ submit/approve ไปแล้วไม่ถูกดึงกลับ) (2) `renderPlotSubmitArea()` เปลี่ยนให้แสดงปุ่มเสมอแม้สถานะเป็น `data_entry` (เป็นปุ่ม disabled + ข้อความ "ต้องบันทึกจุดอ้างอิงและเพิ่มต้นไม้อย่างน้อย 1 ต้นก่อน") แทนที่จะซ่อนไปเฉยๆ — กันความสับสน "หาปุ่มไม่เจอ" ในอนาคตแม้จะเป็นสถานะเริ่มต้นที่ยังกดไม่ได้จริงก็ตาม
- **(v1.26) แก้ไขขอบเขตแปลงที่บันทึกไว้แล้วได้**: เดิมวาดขอบเขตได้แค่ตอนสร้างแปลงใหม่ครั้งเดียว แก้ไขทีหลังไม่ได้ — เพิ่มปุ่ม 🗺️ "แก้ไขขอบเขตแปลง" ในหน้ารายละเอียดแปลง (คู่กับปุ่ม ✏️ แก้ไขข้อมูลแปลงเดิม, ซ่อนทั้งคู่เมื่อแปลงล็อกเหมือนกัน) กด `startEditBoundary(plot)` แล้ว **reuse UI วาดขอบเขตเดิมทั้งหมด** (`#plot-step-boundary` ใน `view-plot-form` — โหมดเดินสำรวจ GPS, ปุ่มลบจุดล่าสุด, ทุกอย่างเหมือนตอนสร้างแปลงใหม่) แทนที่จะสร้าง UI แยกใหม่ — เติม `creatingPlot.points` ด้วยขอบเขตเดิมของแปลง (deep copy) ก่อนเข้าโหมดวาด ตัวแปรใหม่ `editingBoundaryPlotId` แยกแยะ "กำลังแก้ไขขอบเขตแปลงเดิม" ออกจาก "กำลังสร้างแปลงใหม่" (ซึ่งใช้ `creatingPlot`/`boundaryMode` ตัวเดียวกันอยู่แล้ว) — เปลี่ยน label ชั่วคราวตอนอยู่ในโหมดแก้ไข (หัวข้อ "แก้ไขขอบเขตแปลง", ปุ่ม "บันทึกขอบเขตแปลง", sidebar subtitle) แล้วรีเซ็ตกลับเป็นค่าเดิมใน `endBoundaryCreation()` เสมอ — ปุ่มบันทึก/ยกเลิก/ปุ่ม "← กลับ" ด้านบนแตกแขนง logic ตาม `editingBoundaryPlotId`: ถ้ากำลังแก้ไขขอบเขตเดิม จะ **อัปเดตเฉพาะฟิลด์ `boundary`** ของแปลงเดิม (`{...existing, boundary: creatingPlot.points.slice()}`) ไม่แตะฟิลด์อื่นเลย (ชื่อ/เจ้าของ/สถานะ/createdBy ฯลฯ คงเดิมทั้งหมด — ทดสอบยืนยันแล้ว) แล้วกลับไปหน้า `plot-detail` ของแปลงเดิม (ไม่ใช่ `home` เหมือนตอนสร้างแปลงใหม่) — บันทึกผ่าน `PUT /api/plots/:id` เส้นทางเดิม จึงโดน lock check ของ v1.23 บล็อกอัตโนมัติถ้าแปลงส่งตรวจสอบ/ผ่านแล้วอยู่ (ปุ่มก็ถูกซ่อนไว้ตั้งแต่ client อยู่แล้วเป็นชั้นป้องกันซ้ำ) — ทดสอบผ่านเบราว์เซอร์จริงครบ: แก้ไข-บันทึกสำเร็จ (boundary อัปเดตทั้ง local และ server, ฟิลด์อื่นไม่เปลี่ยน), ยกเลิกกลางคันไม่กระทบขอบเขตเดิม, ปุ่มหายไปเมื่อแปลงล็อก
- **(v1.27) ซ่อนบรรทัด "เจ้าของ" บนหน้าแรก/หน้ารายละเอียดแปลง สำหรับแปลงของ user เอง**: ตั้งแต่ v1.15 ชื่อ/เบอร์เจ้าของแปลงถูก lock ให้ตรงกับผู้ใช้ที่ระบุตัวตนอยู่เสมอ ทำให้ทุกการ์ดแปลงของตัวเองแสดง "เจ้าของ: [ชื่อฉันเอง]" ซ้ำๆ ทั้งที่ผู้ใช้รู้อยู่แล้วว่าเป็นข้อมูลตัวเอง (เห็นชื่อตัวเองอยู่แล้วที่เมนู/แถบ identity) — เพิ่ม helper `isOwnPlot(plot)` (เช็ค `plot.createdBy === identifiedUser.id`) ใช้ซ่อนบรรทัด "เจ้าของ: ..." ทั้งใน `renderPlotList()` (การ์ดหน้าแรก) และ `renderPlotDetailInfo()` (banner หน้ารายละเอียดแปลง) **เฉพาะแปลงที่ `createdBy` ตรงกับ user ปัจจุบันเท่านั้น** — แปลงเก่าที่ไม่มี `createdBy` (สร้างก่อน v1.19, ยังมองเห็นได้ทุกคนตามกฎ v1.20) **ยังคงแสดงเจ้าของปกติ** เพราะไม่ใช่ "แปลงของ user เอง" จริงๆ ในความหมายที่ผู้ใช้ขอ (อาจเป็นแปลงของคนอื่นที่ข้อมูลเจ้าของยังมีประโยชน์ต้องดู) — ทดสอบผ่านเบราว์เซอร์จริงแล้วว่าแยกสองกรณีถูกต้อง (แปลงตัวเองไม่โชว์เจ้าของทั้งสองหน้า, แปลงเก่าไม่มี createdBy ยังโชว์เจ้าของปกติ)
- **(v1.28) แยกหน้า admin ออกเป็นเมนูย่อย**: เดิม `Dashboard.jsx` ยัดทุกอย่างไว้หน้าเดียว (info box + แผนที่รวม + ตารางแปลง + ตารางต้นไม้ + จัดการผู้ใช้) ข้อมูลแน่นเกินไป — แยกเป็น 3 หน้าใหม่ตาม sidebar nav ที่ขยายจาก 2 เป็น 5 รายการ (`AdminLayout.jsx` เพิ่ม `NAV_ITEMS`: แดชบอร์ด/แปลงที่ดิน/ต้นไม้/ผู้ใช้งาน[`adminOnly:true`]/วิสาหกิจชุมชน, กรองรายการ nav ที่ render จริงด้วย `navItems = NAV_ITEMS.filter(item => !item.adminOnly || user?.role === 'admin')`): `pages/Plots.jsx` (ย้ายแผนที่รวม + `PlotsTable` มาไว้ที่นี่ทั้งคู่, sync `selectedPlotId` ระหว่างแผนที่กับตารางเหมือนเดิม), `pages/Trees.jsx` (ย้าย `TreesTable` มา, เปลี่ยนตัวกรอง "แสดงเฉพาะแปลงที่เลือก" จาก checkbox เดิม — ซึ่งพึ่ง `selectedPlotId` ร่วมกับแผนที่บนหน้าเดียวกัน — เป็น `<select>` dropdown เลือกแปลงอิสระ เพราะหน้านี้ไม่มีแผนที่ให้ share state ด้วยแล้ว), `pages/Users.jsx` (ห่อ `UsersPanel` ที่ย้ายมาแบบ thin wrapper เดิมไม่เปลี่ยนคอมโพเนนต์) `Dashboard.jsx` เหลือแค่ info box สรุป (แปลง/ต้นไม้/สิทธิ์ปัจจุบัน) + ปุ่ม export GeoJSON ที่ page actions — ทุกหน้าใหม่ (Plots/Trees/Dashboard) ต่างคน fetch ข้อมูลของตัวเองผ่าน `api.listPlots()`/`api.listTrees()` แยกกัน (ไม่ share state ข้ามหน้า) และ scope ข้อมูลสำหรับ `enterprise_admin` ซ้ำในทุกหน้า (filter ด้วย `communityEnterpriseId === user.managedCommunityEnterpriseId`) เหมือนที่ `Dashboard.jsx` เดิมเคยทำ — **หน้า "ผู้ใช้งาน" จำกัดสิทธิ์ที่ระดับ route ด้วย ไม่ใช่แค่ซ่อนจาก nav**: เดิมการป้องกันมีแค่ `{user?.role === 'admin' && <UsersPanel />}` ใน `Dashboard.jsx` ซึ่งหายไปพร้อมกับการย้าย `UsersPanel` ออกมาเป็นหน้าแยก ถ้าไม่เพิ่มการป้องกันใหม่ `enterprise_admin` จะเข้าถึง `/admin/users` ได้ตรงๆถ้ารู้ URL — แก้โดยเพิ่ม prop `requireAdmin` ให้ `ProtectedRoute.jsx` (เดิมเช็คแค่ login แล้ว/role ไม่ใช่ `user`) ถ้า `requireAdmin` เป็น true และ role ไม่ใช่ `admin` จะ `<Navigate to="/" replace />` ทันที, `App.jsx` ใส่ prop นี้เฉพาะ route `/users` เท่านั้น — ทดสอบผ่านเบราว์เศร์จริงครบ: สมัคร admin ทดสอบ + สร้าง plot/tree ทดสอบผ่าน API แล้วเช็คทุกหน้าแสดงผลถูกต้อง (Dashboard เบาลง, Plots มีแผนที่+ตาราง+badge สถานะ, Trees มี dropdown กรองทำงานถูก, Users เข้าได้ปกติในฐานะ admin), สมัคร `enterprise_admin` ทดสอบอีกบัญชีแล้วยืนยันว่า nav ไม่โชว์ "ผู้ใช้งาน" และเข้า URL `/admin/users` ตรงๆถูก redirect กลับ `/` ทันที — `npm run build --prefix admin` ผ่านไม่มี error, `npm test` (backend) ผ่านครบ 33 ข้อ (ไม่กระทบเพราะเป็นงานฝั่ง admin frontend ล้วนๆ)
- **(v1.38) หน้าแก้ไขข้อมูลแปลง แก้ไขสีประจำแปลงได้ด้วย**: เดิม (v1.18) `renderPlotDetailEditForm()` แก้ได้แค่ชื่อ/เอกสารสิทธิ์/เนื้อที่/ที่ตั้ง สีแปลงถูกกำหนดตายตัวตอนสร้างครั้งแรกเท่านั้น (auto-cycle จาก `COLORS[plots.length % COLORS.length]`) แก้ทีหลังไม่ได้เลย — เพิ่มช่อง "สีประจำแปลง" ในฟอร์มแก้ไข ใช้ swatch เดียวกับฟอร์ม "+ เพิ่มแปลงใหม่" (`.color-dot` วนตาม `COLORS` array) แต่**ไม่ได้ใช้ id `#color-row`/ตัวแปร global `editingColor` ร่วมกับฟอร์มเพิ่มแปลงใหม่** (จะชนกันเพราะ `#color-row` เดิมอยู่ในฟอร์ม plot-form ที่ยังอยู่ใน DOM คู่ขนานกันตลอดเวลา ต่างแค่ panel ไหน active) — สร้าง `#edit-color-row` + ตัวแปร `editColor` แบบ local closure ภายใน `renderPlotDetailEditForm()` เอง (เริ่มต้นที่ `plot.color` เดิม) แยกจากกันสนิท ไม่มีทางปนเปื้อนสถานะข้ามฟอร์ม — ปุ่ม "บันทึก" ส่ง `color: editColor` เข้าไปใน object ที่ `PUT /api/plots/:id` เหมือนฟิลด์อื่นๆ (endpoint เดิมรองรับอยู่แล้ว ไม่ต้องแก้ backend เพราะ `color` เป็นคอลัมน์ธรรมดาที่ upsert ปกติไม่ได้ immutable แบบ `created_by`/`status`) — ทดสอบผ่านเบราว์เซอร์จริง: สร้างแปลงสีน้ำเงิน เข้าโหมดแก้ไข ยืนยันว่า swatch เลือกสีน้ำเงินไว้ถูกต้องตามค่าปัจจุบัน เปลี่ยนเป็นสีม่วงแล้วบันทึก ยืนยันค่าที่ได้กลับจาก `GET /api/plots` ตรงกับสีใหม่ และเส้นขอบเขตบนแผนที่หลักเปลี่ยนสีตามทันที ไม่กระทบ backend เลย (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.37) หน้าแรกกลับมาแสดงหมุดจุดอ้างอิงอีกครั้ง (แต่ยังไม่แสดงหมุดต้นไม้)**: ปรับแก้จาก v1.35 ที่เพิ่งซ่อนหมุดจุดอ้างอิงบนหน้าแรกไปด้วย (ตอนนั้นเข้าใจว่าผู้ใช้ต้องการให้เหลือแค่ขอบเขตล้วนๆ) — ผู้ใช้แจ้งกลับมาว่าต้องการเห็นทั้งขอบเขตแปลง**และ**จุดอ้างอิงของทุกแปลงที่เป็นเจ้าของบนหน้าแรกเหมือนก่อน v1.35 (กลับไปใกล้เคียงพฤติกรรมเดิมตั้งแต่ v1.12 ที่ตั้งใจไว้แต่แรก คือหน้าแรกโชว์ขอบเขต+จุดอ้างอิง ซ่อนแค่หมุดต้นไม้อย่างเดียว) — แก้ `renderMarkers()` branch `currentView==='home'`: จากเดิม `return` ทันทีไม่วาดอะไรเลย เปลี่ยนเป็นวนลูป `plots` แล้วเรียก `addRefMarkerToLayer(p)` สำหรับทุกแปลงที่มี `refPoint` และ `isPlotVisibleToUser(p)` เป็นจริง (helper เดียวกับที่ใช้ใน scope อื่นๆ อยู่แล้วตั้งแต่ v1.35 ไม่ต้องเขียนใหม่) ก่อน `return` — ยังไม่วาดหมุดต้นไม้/เส้นทางเชื่อมบนหน้าแรกเหมือนเดิม (ผู้ใช้ไม่ได้ขอกลับส่วนนี้) หน้ารายละเอียดแปลง/หน้าต้นไม้ (v1.35/v1.36) ไม่เปลี่ยนแปลงเลย ยังคงจำกัดเฉพาะแปลงที่เลือกอยู่ — ทดสอบผ่านเบราว์เซอร์จริงด้วยแปลงทดสอบที่มีทั้งขอบเขต+จุดอ้างอิง+ต้นไม้ 1 ต้น ยืนยันด้วยการนับ layer: หน้าแรก = 1 หมุด (จุดอ้างอิง), 0 เส้นทาง, 1 ขอบเขต (ไม่มีหมุดต้นไม้) และเห็นภาพหน้าจอตรงตามนั้น ไม่กระทบ backend (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.36) แผนที่หลักซูมไปให้เห็นขอบเขตแปลงเต็มๆ ทุกครั้งที่เข้าหน้ารายละเอียดแปลง/หน้าต้นไม้**: ต่อเนื่องจาก v1.35 ที่จำกัดแผนที่หลักให้โชว์เฉพาะแปลงที่เลือก — ปัญหาคือแผนที่ไม่ได้ซูม/เลื่อนไปหาแปลงนั้นอัตโนมัติ ทำให้บางครั้งขอบเขตสีแดงอยู่นอกจอหรือเห็นแค่บางส่วน (ค้างตำแหน่ง/ซูมเดิมจากก่อนหน้า) — แก้ไขฟังก์ชัน `fitToPlot()` เดิม (มีอยู่ในโค้ดแต่**ไม่เคยถูกเรียกใช้จริงที่ไหนเลย เป็น dead code**, เดิม fit ตามตำแหน่งต้นไม้เท่านั้นไม่รวมขอบเขต) เปลี่ยนชื่อ/เขียนใหม่เป็น `fitMainMapToSelectedPlot()` — คำนวณ bounds จาก `plot.boundary` ทั้งหมด (ประกันเห็นขอบเขตเต็มแปลงเป็นหลัก ตรงตามที่ผู้ใช้ต้องการ) และ extend ด้วยตำแหน่งจุดอ้างอิงด้วยถ้ามี (เผื่อจุดอ้างอิงอยู่นอกขอบเขตเล็กน้อย) แล้ว `map.fitBounds(bounds.pad(0.15))` — no-op เงียบๆ ถ้าแปลงยังไม่มีขอบเขต (`boundary.length < 3`) ไม่ error — เรียกจาก `setView(view)` ท้ายสุดเมื่อ `view==='plot-detail'` หรือ `'trees'` เท่านั้น (สอดคล้องกับสโคปของ v1.35) ทำงานทุกครั้งที่เข้า/กลับมาหน้านี้ผ่าน `setView()` ไม่ว่าจะมาจากทางไหน (คลิกจากหน้าแรก, กลับจากหน้าต้นไม้, login/register แล้ว auto-redirect ตอน purpose `edit-plot` ฯลฯ) เพราะรวมไว้ที่จุดเดียวใน `setView()` ไม่ต้องเพิ่ม manual call แยกทีละจุด — ทดสอบผ่านเบราว์เซอร์จริง: ตั้งแผนที่ไปตำแหน่งไกลๆ (`[0,0]` zoom 3) ก่อนเข้าหน้ารายละเอียดแปลง/หน้าต้นไม้ ยืนยันว่า `map.getCenter()`/`getZoom()` เปลี่ยนไปตรงกลางขอบเขตแปลงถูกต้องทั้งสองหน้า และภาพหน้าจอเห็นกรอบแดงเต็มพร้อมระยะขอบ (padding) รอบๆ พอดี, ทดสอบ edge case แปลงที่ยังไม่มีขอบเขตเลย (`boundary: []`) ไม่ error และแผนที่ไม่ขยับ (no-op ตามคาด) ไม่กระทบ backend (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.35) หน้าแรกไม่แสดงหมุดต้นไม้/จุดอ้างอิง + หน้ารายละเอียดแปลง/หน้าต้นไม้ แผนที่หลักจำกัดแสดงเฉพาะแปลงที่เลือก**: เดิม (ตั้งแต่ v1.12) แผนที่หลักหน้าแรกซ่อนเฉพาะหมุดต้นไม้ ยังคงแสดงหมุดจุดอ้างอิง (🚩) ของทุกแปลงที่มองเห็นได้ — ผู้ใช้ต้องการให้หน้าแรกโล่งขึ้น เหลือแค่เส้นขอบเขตแปลงล้วนๆ ไม่มีหมุดใดๆ เลย — และหน้ารายละเอียดแปลง/หน้าต้นไม้ (ซึ่งเดิมแผนที่หลัก — ไม่ใช่แผนที่ย่อ `#plot-detail-map` — แสดงหมุด+เส้นทาง+ขอบเขตของ**ทุกแปลง**ที่เป็นเจ้าของ ไม่ได้กรองตามแปลงที่กำลังดูอยู่) ให้จำกัดแสดงเฉพาะแปลงที่กำลังเลือก (`selectedPlotId`) เท่านั้น ตัดความรกตอนโฟกัสทำงานกับแปลงเดียว (ถามผู้ใช้แล้วว่าจะจำกัดเฉพาะหน้ารายละเอียดแปลงหรือหน้าต้นไม้ด้วย เลือก "ทั้งสองหน้า") — `renderMarkers()` แยก 3 กรณีตาม `currentView`: `'home'` → return ทันทีไม่วาดอะไรเลย (มาร์คไว้ในคอมเมนต์ว่าตั้งใจให้เห็นแค่ขอบเขตจาก `renderBoundaries()`), `'plot-detail'`/`'trees'` → หาแปลงจาก `selectedPlotId` ตัวเดียว วาดหมุดต้นไม้/เส้นทาง/หมุดจุดอ้างอิงเฉพาะแปลงนั้น, กรณีอื่น (เช่น `'plot-form'` ตอนวาดขอบเขตแปลงใหม่/แก้ไข) → พฤติกรรมเดิมคือวาดของทุกแปลงที่มองเห็นได้ (ตั้งใจคงไว้ เพื่อช่วยเทียบตำแหน่งกับแปลงอื่นตอนวาดขอบเขตใหม่กันซ้อนทับ ไม่ใช่ scope ของงานนี้) — ปรับ `renderBoundaries()` ให้กรองด้วยเงื่อนไขเดียวกัน (`currentView==='plot-detail'||'trees'` → เหลือแค่ `p.id===selectedPlotId`) — สกัด logic การสร้าง marker/path ที่ซ้ำกันเดิม (มี 3 ที่คัดลอกโค้ดเหมือนกัน) ออกเป็น helper `addTreeMarkerToLayer(t,plot)`, `addRefMarkerToLayer(p)`, `addPathToLayer(p,plotTrees)` เรียกใช้ร่วมกันทั้ง 3 กรณีใน `renderMarkers()` — **เจอปัญหาสำคัญระหว่าง dev**: `setView(view)` เดิมเรียกแค่ `renderMarkers()` ไม่เรียก `renderBoundaries()` (เป็น known gotcha ที่เคยบันทึกไว้ตั้งแต่ v1.20 ว่าต้องเพิ่ม `renderBoundaries()` แยกเองที่ `openAddPlotFlow()`) พอ `renderBoundaries()` เริ่มพึ่ง `currentView` เพื่อ scope ด้วยแล้ว การไม่เรียกอัตโนมัติทำให้ขอบเขตแปลงค้างของ view เก่าเวลาสลับหน้า (เช่น กดเลือกแปลงจากหน้าแรกเข้า `plot-detail` ขอบเขตยังโชว์ทุกแปลงอยู่จนกว่าจะมี trigger อื่นมาสั่ง render ซ้ำ) — แก้โดยเพิ่ม `renderBoundaries()` เข้าไปใน `setView()` ให้เรียกคู่กับ `renderMarkers()` เสมอ (ลบ workaround เดิมที่ `openAddPlotFlow()` ออกด้วยเพราะซ้ำซ้อนแล้ว) แก้ gotcha นี้ให้หมดไปทั้งระบบแทนที่จะเพิ่ม manual call ทีละจุดเหมือนที่ผ่านมา — ทดสอบผ่านเบราว์เซอร์จริงด้วยการสร้าง 2 แปลง (แต่ละแปลงมีจุดอ้างอิง+ต้นไม้ 2 ต้น) เป็นของ user เดียวกัน ยืนยันด้วยการนับ layer จริงบนแผนที่ (ผ่าน `markerLayer.eachLayer()`/`boundaryLayer.eachLayer()`): หน้าแรก = 0 หมุด, 0 เส้นทาง, 2 ขอบเขต (ถูกต้อง), เลือกแปลง 1 เข้า plot-detail = 3 หมุด(จุดอ้างอิง+ต้นไม้ 2)+1 เส้นทาง+1 ขอบเขต (เฉพาะแปลง 1), สลับไปหน้าต้นไม้ยังคงถูกสโคปเหมือนกัน, สลับไปเลือกแปลง 2 ขอบเขต/หมุดอัปเดตเป็นของแปลง 2 ถูกต้อง (ยืนยันด้วย tooltip ชื่อแปลงบน polygon), กลับหน้าแรกขอบเขตกลับมาแสดงครบ 2 แปลงเหมือนเดิม — ไม่กระทบ backend (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.34) สีขอบเขตแปลง/เส้นทาง/หมุด ตัดกับพื้นหลังสีเขียวของภาพถ่ายดาวเทียม**: `COLORS` palette (สีประจำแปลง สุ่มวนตามลำดับสร้างแปลง ใช้ทั้งเป็นสีขอบเขตบนแผนที่และสี swatch ในรายการแปลง) เดิมมี 2 จาก 8 สีเป็นโทนเขียว (`#3D6B4A` ตรงกับ `--primary` ของธีมแอปพอดี, `#5A8A6B`) ซึ่งกลืนกับภาพถ่ายดาวเทียมที่ส่วนใหญ่เป็นสีเขียว (พืชพรรณ/ต้นไม้) แทบมองไม่เห็นเส้นขอบเขต — แทนที่ 2 สีนี้ด้วยสีแดง `#C0392B` และสีชมพูบานเย็น `#C2185B` (อีก 6 สีเดิม: น้ำตาล/น้ำเงิน/ม่วง/สนิม/ทอง/เขียวอมฟ้า ยังคงไว้ ไม่กระทบ) — **เดิมเส้นทาง (เส้นประเชื่อมจุดอ้างอิง→ต้นไม้ จาก v1.33) ใช้สีเดียวกับเส้นขอบเขต (`plot.color`) ทำให้แยกไม่ออกว่าเส้นไหนคือเส้นทางเดินสำรวจ เส้นไหนคือขอบเขตแปลง** — แก้โดยแยกเส้นทางออกมาใช้สีคงที่ต่างหากไม่ผูกกับสีแปลง: ค่าคงที่ใหม่ `PATH_LINE_COLOR = '#2563EB'` (น้ำเงินสด, ตัดกับทั้งพื้นหลังเขียวและสีแดง/ส้มของหมุด/ขอบเขต) ใช้แทน `p.color`/`plot.color` ทั้งใน `renderMarkers()` (แผนที่หลัก) และ `renderPlotDetailMap()` (แผนที่ย่อ) เพิ่ม weight จาก 2.5→3 และ opacity 0.8→0.9 ให้เห็นชัดขึ้นด้วย — **สำคัญ**: การเปลี่ยนค่าใน `COLORS` array มีผลแค่กับ**แปลงใหม่ที่สร้างหลังจากนี้เท่านั้น** (`editingColor = COLORS[plots.length % COLORS.length]`) เพราะ `plot.color` ถูกบันทึกเป็นค่า hex ตรงๆ ลง DB ตอนสร้างแปลง ไม่ได้อ้างอิง index ของ array แบบ dynamic แปลงเก่าที่เคยได้สีเขียวไปแล้วจะยังเป็นสีเขียวเหมือนเดิม (ไม่ retroactive) — หมุดต้นไม้/จุดอ้างอิง (ธงส้ม/แดง จาก v1.31) ไม่ต้องแก้เพราะเป็นสีคงที่ตัดกับพื้นเขียวอยู่แล้วตั้งแต่ต้น — ทดสอบผ่านเบราว์เซอร์จริงด้วยแปลงทดสอบสีแดง (`#C0392B`) ยืนยันภาพหน้าจอเห็นเส้นขอบเขตแดงกับเส้นทางน้ำเงินแยกจากกันชัดเจนบนพื้นหลังดาวเทียมสีเขียว และตรวจสอบค่าสีจริงของ layer ผ่าน JS ตรงกับที่กำหนดไว้ ไม่กระทบ backend (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.33) ขีดเส้นทางจากจุดอ้างอิงไปยังต้นไม้ตามลำดับ (จุดอ้างอิง → ต้นที่ 1 → ต้นที่ 2 → ...)**: เพิ่ม helper กลาง `plotPathLatLngs(plot, plotTrees)` คืน array พิกัด `[lat,lng]` เรียงลำดับ: จุดอ้างอิง (ถ้ามี) ตามด้วยต้นไม้ทุกต้นในแปลงเรียงตาม `seq` จากน้อยไปมาก แล้ววาดด้วย `L.polyline` (สีตามสีแปลง, เส้นประ `dashArray:'6,6'`, weight 2.5, opacity 0.8 — แยกจากเส้นขอบเขตที่เป็นเส้นทึบ/หนากว่า กันสับสนสองเส้นนี้) ใช้ฟังก์ชันเดียวกันทั้ง 2 จุด: `renderMarkers()` (แผนที่หลัก, วาดเฉพาะ `currentView !== 'home'` เหมือนเงื่อนไขเดิมของหมุดต้นไม้ เพราะถ้าซ่อนต้นไม้อยู่แล้วเส้นทางก็ไม่มีประโยชน์ให้เห็น) และ `renderPlotDetailMap()` (แผนที่ย่อหน้ารายละเอียดแปลง, วาดเสมอไม่ขึ้นกับ view เพราะเป็นแผนที่เฉพาะแปลงเดียวอยู่แล้ว) ไม่วาดเส้นถ้ามีจุดน้อยกว่า 2 จุด (เช่น มีแค่จุดอ้างอิงแต่ยังไม่มีต้นไม้เลย) — ทดสอบด้วยการสร้างแปลงทดสอบ+จุดอ้างอิง+ต้นไม้ 3 ต้นผ่าน API ตรง แล้วยืนยันพิกัดของ polyline ที่ได้ตรงกับลำดับที่คาดไว้เป๊ะทั้งแผนที่หลักและแผนที่ย่อ (`ref→1→2→3`) เป็นการเปลี่ยนแปลงฝั่ง client ล้วนๆ ไม่กระทบ backend (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.32) แสดงความแม่นยำ GPS + วงกลมรัศมีทุกครั้งที่อ่านตำแหน่ง**: ผู้ใช้แจ้งว่า GPS คลาดเคลื่อนกับภาพถ่ายดาวเทียมสูง — จริงๆ แล้วเป็นข้อจำกัดของ GPS โทรศัพท์เอง (5-20 ม.ขึ้นไปถ้าอยู่ใต้ร่มไม้/สิ่งปลูกสร้างบัง) ไม่ใช่บั๊ก แต่แอปเดิมไม่เคยบอกผู้ใช้เลยว่าค่าที่อ่านได้ ณ ขณะนั้นแม่นยำแค่ไหน จึงเพิ่มการแสดงผลความแม่นยำเข้าไป — ฟังก์ชันกลาง `accuracyCircleStyle(accuracy)` คืน style ของ `L.circle` (สีเขียว `#3D6B4A` ถ้าความแม่นยำ ≤`GPS_ACCURACY_WARN_THRESHOLD` (20 เมตร), สีแดง `#DC2626` ถ้าแย่กว่านั้น) ใช้ร่วมกัน 2 จุด: (1) `locateUser()` — ฟังก์ชันกลางที่ใช้ทั้งปุ่ม 📍 GPS หลัก, ปุ่ม "ใช้ตำแหน่ง GPS ปัจจุบัน" ใน modal เพิ่ม/แก้ไขต้นไม้และจุดอ้างอิง, และ auto-locate ตอน login/boot — วาด `L.circle` รัศมี = `pos.coords.accuracy` รอบหมุด GPS (`userAccuracyCircle`, ลบตัวเก่าทิ้งก่อนวาดใหม่ทุกครั้งเหมือน `userMarker`) พร้อม `bindTooltip` บอกค่าความแม่นยำเป็นเมตร ถ้าต่ำกว่าเกณฑ์จะมีข้อความเพิ่ม "รอสักครู่ให้ GPS นิ่งขึ้น หรือใช้ 'เลือกบนแผนที่' แทน" (ชี้ทางออกไปที่ปุ่ม 🗺️ เลือกบนแผนที่ ที่มีอยู่แล้วตั้งแต่ v1.13 ซึ่งแม่นยำกว่าเพราะอิงตำแหน่งจริงบนภาพถ่ายดาวเทียมโดยตรง) เปิด tooltip อัตโนมัติเฉพาะตอนไม่ใช่ `silent` mode (คือกดปุ่มเองเท่านั้น ไม่โชว์ตอน auto-locate เงียบๆ ตอน boot/login) — **ไม่ block การใช้งาน** แค่เตือนเฉยๆ เพราะบางพื้นที่ (เช่นในป่า) อาจไม่มีทางได้ signal ดีกว่านี้เลย การบังคับรอไม่ช่วยอะไร (2) `startWalkTracking()`/`stopWalkTracking()` (โหมดเดินสำรวจวาดขอบเขต v1.10) — วาดวงกลมรัศมีรอบหมุดเดินสำรวจ (`walkAccuracyCircle`) อัปเดตทุกครั้งที่ `watchPosition` ยิง callback ใหม่ (ไม่มี tooltip เพราะจะกะพริบถี่เกินไปตอนเดิน แค่วงกลมสีให้เห็นเชิงภาพว่าจุดที่กำลังบันทึกขอบเขตความน่าเชื่อถือแค่ไหน) `stopWalkTracking()` ล้างวงกลมทิ้งด้วยเหมือน marker เดิม — ทดสอบผ่านเบราว์เซอร์จริงด้วยการ mock `navigator.geolocation.getCurrentPosition`/`watchPosition` ให้คืนค่า accuracy ทั้งกรณีดี (8 ม. → วงกลมเขียว ไม่มีคำเตือน) และแย่ (45 ม. → วงกลมแดง + tooltip คำเตือนเปิดอัตโนมัติ) ยืนยันทั้งค่า radius/สี/ข้อความ tooltip ถูกต้อง และยืนยัน cleanup ของวงกลมทั้งสองโหมดทำงานถูกต้องไม่มี layer ค้าง — เป็นการเปลี่ยนแปลงฝั่ง client ล้วนๆ ไม่กระทบ backend (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.31) เปลี่ยนหมุดต้นไม้/จุดอ้างอิงบนแผนที่จากวงกลมสีตามแปลงเป็นธง**: เดิมหมุดต้นไม้ (`.tree-pin`) เป็นทรง pin หยดน้ำ (border-radius 50% 50% 50% 0 หมุน -45deg ดูคล้ายวงกลม) พื้นหลังสีตามสีแปลง (`plot.color`, ส่วนใหญ่เป็นสีเขียวจาก `COLORS[0]`) มีเลขลำดับต้นไม้อยู่ข้างใน และหมุดจุดอ้างอิง (`.ref-pin`) เป็นวงกลมสีตามแปลงเช่นกันมี 🚩 อยู่ข้างใน — ผู้ใช้ต้องการเปลี่ยนเป็นธงสีคงที่แทน ไม่อิงสีแปลงอีกต่อไป: **จุดอ้างอิง = ธงสีแดง, ต้นไม้ = ธงสีส้ม** — สร้าง class ใหม่ `.flag-pin` (ธงบนเสา วาดด้วย CSS ล้วน ไม่ใช้ emoji เพราะคุมสีแม่นยำกว่า: เสาเป็น `div.pole` สีน้ำตาลเข้ม, ตัวธงเป็น `div.flag-tri` สามเหลี่ยมด้วยเทคนิค `border` trick มี `filter:drop-shadow` แทนเงาเดิม) แยกสีด้วย modifier class `.flag-pin.ref` (แดง `#DC2626`) กับ `.flag-pin.tree` (ส้ม `#F97316`) — หมุดต้นไม้ยังคงแสดงเลขลำดับกำกับอยู่ (ถามผู้ใช้ผ่าน AskUserQuestion แล้วเลือก "แสดงเลขกำกับข้างธงด้วย") ผ่าน `<div class="tree-flag-label">` ป้ายกลมมุมมนขาวขอบส้มวางด้านขวาของธง ไม่ใช่ตัวเลขในตัวธงเหมือนเดิม — สร้างฟังก์ชันช่วย `treeFlagIcon(seq)`/`refFlagIcon()` (คืนค่า `L.divIcon`) ใช้ร่วมกันทั้งแผนที่หลัก (`renderMarkers()`) และแผนที่ย่อในหน้ารายละเอียดแปลง (`renderPlotDetailMap()`) กันโค้ดซ้ำ (เดิมมี icon markup ซ้ำกัน 4 จุด) — ลบ CSS `.tree-pin`/`.ref-pin` เดิมออกทั้งหมด (ไม่มีจุดใช้งานเหลือ) — เป็นการเปลี่ยนแปลงฝั่ง client ล้วนๆ ไม่กระทบ backend/schema, ทดสอบผ่านเบราว์เซอร์จริงด้วยการสร้างแปลงทดสอบพร้อมจุดอ้างอิง+ต้นไม้ 3 ต้นผ่าน API ตรง แล้วยืนยันภาพหน้าจอว่าธงแดง/ส้มแสดงถูกสีและเลขลำดับต้นไม้ยังกำกับอยู่ครบทั้งแผนที่หลักและแผนที่ย่อ (`npm test` ผ่านครบ 35 ข้อเหมือนเดิมเพราะไม่แตะ backend)
- **(v1.30) หน้าระบุตัวตนก่อนเพิ่มแปลง เข้าสู่ระบบด้วยรหัสผ่านได้เพิ่มจากวันเกิด**: เดิม (v1.14/v1.21) modal ระบุตัวตนบังคับ login ด้วยวันเกิดเท่านั้น (แปลงเป็น password ผ่าน `parseThaiDob()`) — ปัญหาคือตั้งแต่ v1.29 ที่ admin เปลี่ยนรหัสผ่านของ user ผ่านหน้า admin ได้ตรงๆ (`ChangePasswordModal`) รหัสผ่านจริงใน DB จะไม่ตรงกับวันเกิดอีกต่อไป ทำให้ field worker คนนั้น login ด้วยวันเกิดไม่ได้อีกเลย — เพิ่มปุ่มสลับโหมด 2 ปุ่ม (`.identify-mode-toggle`: "เข้าสู่ระบบด้วยวันเกิด" / "เข้าสู่ระบบด้วยรหัสผ่าน") เหนือช่องกรอก สลับด้วย `setIdentifyLoginMode(mode)` (toggle class `active` + ซ่อน/โชว์ `#identify-dob-field` กับ `#identify-password-field` สลับกัน, ตัวแปร global `identifyLoginMode` เก็บโหมดปัจจุบัน) ปุ่ม "เข้าสู่ระบบ" อ่านค่าตามโหมด: โหมดวันเกิดยังผ่าน `parseThaiDob()` เหมือนเดิมทุกประการ, โหมดรหัสผ่านส่งค่าจากช่อง `#in-identify-password` ตรงๆไม่แปลงอะไร — ข้อความ error ตอน login ไม่ผ่านปรับให้บอกสาเหตุตามโหมดด้วย ("วันเกิดไม่ตรง" หรือ "รหัสผ่านไม่ถูกต้อง") — **ฟอร์ม "ลงทะเบียนใหม่" ยังคงใช้วันเกิดอย่างเดียวเหมือนเดิม ไม่มีตัวเลือกรหัสผ่าน** (สมเหตุสมผลเพราะผู้ใช้ใหม่ยังไม่มีรหัสผ่านที่ admin ตั้งให้) กดปุ่ม "ยังไม่เคยลงทะเบียน? กดที่นี่" จะบังคับสลับกลับโหมดวันเกิดและซ่อนปุ่มสลับโหมดไปเลยเพื่อกันสับสน (`btn-identify-show-register` handler เรียก `setIdentifyLoginMode('dob')` + ซ่อน `.identify-mode-toggle`) `openIdentifyModal()` reset กลับเป็นโหมดวันเกิด + โชว์ปุ่มสลับโหมดคืนทุกครั้งที่เปิด modal ใหม่ (safety net กันเข้าซ้ำแล้วค้างสถานะเดิม) — ทดสอบผ่านเบราว์เซอร์จริงครบ: register field user ด้วยวันเกิด → admin เปลี่ยนรหัสผ่านผ่านหน้า admin (v1.29) → login ด้วยวันเกิดเดิม (fail ตามคาด) → สลับโหมดรหัสผ่าน → login ด้วยรหัสผ่านใหม่สำเร็จ, กดลงทะเบียนใหม่ยืนยันว่ากลับเป็นโหมดวันเกิดและซ่อนตัวเลือกถูกต้อง — เป็นการแก้ไขฝั่ง client ล้วนๆ ไม่กระทบ backend/schema เลย (`npm test` ผ่านครบ 35 ข้อเหมือนเดิม)
- **(v1.39) เมนู admin "แปลงที่ดิน" เปลี่ยนชื่อเป็น "แปลงต้นไม้" + แผนที่/ตารางแยกเป็นแท็บ**: `AdminLayout.jsx` `NAV_ITEMS` label เปลี่ยนจาก "แปลงที่ดิน" → "แปลงต้นไม้" (ถามผู้ใช้แล้วว่าจะรวมแผนที่+ตารางไว้การ์ดเดียวกันแบบสลับแท็บ หรือแยกเป็นแท็บอิสระคนละการ์ด เลือก "แยกเป็นแท็บ สลับดูได้ทีละอัน") `pages/Plots.jsx` เปลี่ยนจากวาง Card "แผนที่แปลงที่ดิน" กับ Card "รายการแปลง" เรียงต่อกันแนวตั้งเสมอ (ต้อง scroll ผ่านแผนที่สูงๆ กว่าจะเห็นตาราง) เป็น Card เดียวมีแถบแท็บด้านบน (`🗺️ แผนที่` / `📋 ตาราง (N)`) สลับดูได้ทีละอัน ผ่าน state `activeTab` ('map'|'table') ธรรมดา ไม่ใช้ library แท็บเพิ่ม — สไตล์แท็บ underline สีเขียวใต้แท็บที่ active (border-b-2 border-emerald-600) ตรงกับโทนสีเขียวเดิมของธีม admin แผนที่ตอนนี้สูงขึ้นเป็น `h-[34rem]` (เดิม h-96) เพราะไม่ต้องแบ่งพื้นที่ให้ตารางที่อยู่ข้างล่างอีกต่อไป — ปุ่ม/ฟังก์ชันของตาราง (แก้ไข/จัดการสถานะ/ลบ/sync selectedPlotId กับแผนที่) ไม่เปลี่ยนแปลงเลย ยังทำงานเหมือนเดิมทุกอย่างแค่ย้ายมาอยู่คนละแท็บ — ทดสอบผ่านเบราว์เซอร์จริง: เมนู sidebar โชว์ "แปลงต้นไม้" ถูกต้อง, หน้าเปิดมาแท็บแผนที่ active เห็นขอบเขตแปลงสีตามที่กำหนด, คลิกแท็บตารางสลับไปเห็นตารางพร้อมปุ่มจัดการครบ (แก้ไข/จัดการสถานะ/ลบ) — เป็นงานฝั่ง admin frontend ล้วนๆ `npm run build --prefix admin` ผ่านไม่มี error, `npm test` (backend) ผ่านครบ 35 ข้อเหมือนเดิม (ไม่กระทบ)
- **(v1.40) หน้า admin "แปลงต้นไม้" เพิ่มตัวกรองข้อมูล**: ถามผู้ใช้ว่าต้องการตัวกรองแบบไหน (multi-select) เลือกมา 3 อย่าง: ค้นหาข้อความอิสระ (ชื่อแปลง/ชื่อเจ้าของ/เบอร์โทร รวมกันเป็น haystack เดียว lowercase substring match), กรองตามสถานะ (dropdown ใช้ label ชุดเดียวกับ `PlotStatusModal.jsx`), กรองตามวิสาหกิจชุมชน (dropdown, **ซ่อนไว้ถ้า role เป็น `enterprise_admin`** เพราะกลุ่มเดียวเสมออยู่แล้วกรองไปก็ไม่มีความหมาย) — ทั้ง 3 ตัวกรอง apply ร่วมกันแบบ AND (`filteredPlots` ใน `Plots.jsx`) มีผลกับทั้ง 2 แท็บพร้อมกัน (แผนที่+ตาราง ใช้ list เดียวกัน ไม่ใช่กรองแยกกันคนละแท็บ) `filteredTrees` ตามมาด้วย (กรองเฉพาะต้นไม้ของแปลงที่ผ่านตัวกรอง ไม่ใช่ทุกต้นไม้ที่ scope เห็น) — subtitle ใต้หัวข้อเปลี่ยนข้อความเป็น "แสดง N จากทั้งหมด M แปลง" เมื่อมีตัวกรองใช้งานอยู่ (ปกติ "ทั้งหมด M แปลง" เหมือนเดิม) มีปุ่ม "ล้างตัวกรอง" โผล่มาเฉพาะตอนมีตัวกรองใช้งานอยู่อย่างน้อย 1 ตัว รีเซ็ตทั้ง 3 ค่าพร้อมกัน — เพิ่ม state `filteredPlots.length === 0` แสดงข้อความ "ไม่พบแปลงที่ตรงกับตัวกรอง" แทนแผนที่/ตารางว่างเปล่า — ต้อง fetch `api.listCommunityEnterprises()` เพิ่มใน `reload()` ของหน้านี้ (เดิมไม่เคย fetch เพราะไม่เคยต้องใช้) เอนด์พอยต์เดิม `GET /api/admin/community-enterprises` scope ให้ตามสิทธิ์อยู่แล้ว (`enterprise_admin` เห็นแค่กลุ่มตัวเอง — ไม่ต้อง handle เพิ่มฝั่ง client เพราะซ่อน dropdown นี้ทั้งอันไปแล้วสำหรับ role นี้) — ทดสอบผ่านเบราว์เซอร์จริงครบ: สร้าง 3 แปลงทดสอบ (ชื่อ/เจ้าของ/สถานะ/วิสาหกิจชุมชนต่างกัน) ค้นหาด้วยชื่อเจ้าของกรองเหลือ 1 ถูกต้อง, กรองสถานะเหลือ 1 ถูกต้อง, กรองวิสาหกิจชุมชนเหลือ 1 ถูกต้อง, ปุ่มล้างตัวกรองคืนค่าครบ 3 แปลงถูกต้อง เป็นงานฝั่ง admin frontend ล้วนๆ `npm run build --prefix admin` ผ่านไม่มี error, `npm test` (backend) ผ่านครบ 35 ข้อเหมือนเดิม (ไม่กระทบ)
- **เว็บ admin**: หน้า `/admin` (React) ครบตามแผนเดิมใน CLAUDE.md — สมัคร/login (เปิดสมัครเองอิสระ), ดูแผนที่รวมทุกแปลง+ต้นไม้, ตารางแปลง/ต้นไม้แบบแก้ไข inline + ลบ, ปุ่ม export CSV (แปลง/ต้นไม้แยกไฟล์) และ GeoJSON (polygon แปลง + point ต้นไม้ รวมไฟล์เดียว) ทุก export endpoint ต้อง login (`requireAuth` middleware ใน `server/auth.js`) — ตั้งแต่ v1.28 ข้อมูลเหล่านี้แยกไปอยู่คนละหน้า/เมนูตาม sidebar แล้ว (ดูหัวข้อ v1.28)
- **หน้าตา admin แบบ AdminLTE**: รีสไตล์ทั้งแอป `/admin` ให้หน้าตาเหมือน [AdminLTE](https://adminlte.io/) (sidebar เมนูสีเข้ม + navbar ด้านบน + เนื้อหาเป็นกล่อง card) **แต่ยังใช้ React + Tailwind เดิมทั้งหมด ไม่ได้ติดตั้ง AdminLTE library จริง** (ตัดสินใจแล้วว่าจะชนกับ React ถ้าใช้ตัวจริงที่พึ่ง jQuery/Bootstrap) — component ใหม่ที่เพิ่ม: `AdminLayout.jsx` (sidebar เมนูสีกรมท่า `bg-slate-900` พับ/ขยายได้ผ่านปุ่ม ☰ บน navbar, state `collapsed` เก็บใน component เอง ไม่ persist ข้าม reload, เมนู nav ไฮไลต์ active ด้วย `useLocation()`, ส่วนล่าง sidebar โชว์ identity + badge role), `Card.jsx` (กล่องเนื้อหามาตรฐาน มี header แถบเทาอ่อนพร้อม title/headerRight + body, prop `noPadding` สำหรับใส่ตาราง/แผนที่ที่ไม่ต้องการ padding รอบ), `InfoBox.jsx` (stat box สีพื้นเต็ม ไอคอน+ตัวเลข+label แบบ AdminLTE small-box, มี 4 สี blue/green/amber/slate), `PageHeader.jsx` (แถบหัวข้อหน้า + subtitle + ปุ่ม actions ด้านขวา) — `App.jsx` เปลี่ยนให้ route `/` และ `/community-enterprises` ห่อด้วย `<AdminLayout>` แทนที่แต่ละหน้าจะมี `<header>` ของตัวเอง (ลบ header เดิมออกจาก `Dashboard.jsx`/`CommunityEnterprises.jsx` แล้ว) `Dashboard.jsx` เพิ่ม info box สรุปจำนวนแปลง/ต้นไม้/สิทธิ์ปัจจุบันไว้บนสุด ย้ายปุ่ม export จาก navbar เดิมไปเป็น page actions แทน — `PlotsTable.jsx`/`TreesTable.jsx`/`UsersPanel.jsx`/`CommunityEnterpriseCard.jsx` ถูกห่อด้วย `Card` ทั้งหมด (ตารางใช้ `noPadding` + ลบ wrapper `rounded-xl border` เดิมของตัวเองออกเพราะซ้ำกับที่ `Card` ให้แล้ว) `Login.jsx`/`Register.jsx` ปรับเป็นสไตล์ login-box ของ AdminLTE (โลโก้/ชื่อแอปลอยเหนือกล่องฟอร์ม แทนที่จะอยู่ในหัวกล่องเดิม) — โทนสียังคงธีมเขียว emerald เดิมของแอปไว้ (ไม่ได้เปลี่ยนเป็นน้ำเงินตามค่า default ของ AdminLTE) ทดสอบผ่านเบราว์เซอร์จริงแล้ว: sidebar พับ/ขยายทำงานถูกต้อง, ทุกหน้า (dashboard, วิสาหกิจชุมชน, ฟอร์มแก้ไข, users panel) แสดงผลตรงตามดีไซน์ที่ตั้งใจ — **หน้า login ตัดลิงก์ "สมัครสมาชิก" ออกแล้ว** (`Login.jsx` ไม่มี `<Link to="/register">` อีกต่อไป) เป็นการซ่อนแค่ทางเข้าจากหน้า login เท่านั้น route `/admin/register` และหน้า `Register.jsx` เองยังอยู่ครบ ยังเข้าได้ถ้ารู้ URL ตรงๆ (ระบบสมัครสมาชิก admin ยังเปิดอิสระเหมือนเดิมตามที่ตัดสินใจไว้ก่อนหน้า ไม่ได้ปิดฟีเจอร์ ปิดแค่การโปรโมทลิงก์)
- **จัดการผู้ใช้จากหน้า admin**: admin ที่ login อยู่เพิ่มผู้ใช้ใหม่ได้เอง (กรอกอีเมล+รหัสผ่านให้ผู้ใช้ใหม่โดยตรง ไม่ต้องรอสมัครเอง) ผ่าน `UsersPanel` ใน dashboard — endpoint `GET/POST /api/admin/users` (ต้อง `requireAuth`) `POST` **ไม่ตั้ง session cookie ให้ตัวเอง** (สำคัญ: ต้องไม่ทำให้ admin ที่กำลังเพิ่มคนอื่นหลุด session ตัวเอง) เทียบกับ `/api/auth/register` ที่ตั้ง cookie ให้ตอนคนสมัครเอง
- **(v1.29) หน้า "ผู้ใช้งาน" เป็นตาราง กรองตามวิสาหกิจชุมชนได้ + แก้ไขข้อมูล/เปลี่ยนรหัสผ่านได้**: `UsersPanel.jsx` เปลี่ยนจาก `<ul>` list ธรรมดา (มีแค่ inline select เปลี่ยน role/CE ต่อแถว ไม่มีปุ่มแก้ไขข้อมูลอื่น/เปลี่ยนรหัสผ่าน) เป็นตาราง (คอลัมน์ ชื่อ/อีเมล/เบอร์โทร, สิทธิ์, วิสาหกิจชุมชน, วันที่สร้าง, จัดการ) พร้อม dropdown กรอง "ทุกวิสาหกิจชุมชน" หรือเลือกทีละกลุ่ม — **"สังกัดวิสาหกิจชุมชน" ของ user หนึ่งคนนับจาก 2 ความสัมพันธ์รวมกัน**: เป็นสมาชิก (`community_enterprise_members`, ผูกผ่านหน้า "วิสาหกิจชุมชน") **หรือ** เป็น `enterprise_admin` ที่ดูแลกลุ่มนั้นอยู่ (`managedCommunityEnterpriseId`) — helper `affiliatedEntityIdsById` รวมทั้งสองแบบเข้าด้วยกัน (พลาดไป 1 รอบระหว่าง dev ที่นับแค่ความเป็นสมาชิกอย่างเดียว ทำให้กรองแล้วไม่เห็น enterprise_admin ของกลุ่มนั้นเลย) ทั้งคอลัมน์แสดงผลและตัวกรองใช้ helper เดียวกันนี้ — ปุ่ม "+ เพิ่มผู้ใช้ใหม่" เปลี่ยนจากฟอร์มแทรกอยู่บนตารางตลอดเวลา เป็นเปิด modal (`AddUserModal.jsx`, เลือก role ตอนเพิ่มได้เลยรวมถึง `enterprise_admin`+กลุ่มที่ดูแล ไม่ต้องเพิ่มแล้วมาเปลี่ยน role ทีหลังเหมือนเดิม), ปุ่ม "แก้ไข" ต่อแถวเปิด `EditUserModal.jsx` (รวมการแก้ชื่อ/อีเมล/เบอร์โทร/role/กลุ่มที่ดูแล ไว้ในที่เดียว — เดิม role/กลุ่มแก้ได้ผ่าน inline select แต่ชื่อ/อีเมล/เบอร์โทรแก้ไม่ได้เลยตั้งแต่สร้าง), ปุ่ม "เปลี่ยนรหัสผ่าน" เปิด `ChangePasswordModal.jsx` (admin ตั้งรหัสผ่านใหม่ให้ user คนอื่นได้ตรงๆ โดยไม่ต้องรู้รหัสเดิม, validate ความยาว+ตรงกันทั้งสองช่องฝั่ง client) — เพิ่ม endpoint ใหม่ 2 ตัว (ทั้งคู่ `requireAdmin`, ไม่เปิดให้ `enterprise_admin`): `PATCH /api/admin/users/:id` (แก้ name/email/phone — validate format เดียวกับตอนสมัคร, ต้องเหลืออย่างน้อย email หรือ phone อย่างใดอย่างหนึ่งเสมอ, เช็ค unique email/phone ไม่ชนกับ user อื่นแต่ไม่ชนกับตัวเอง) และ `PATCH /api/admin/users/:id/password` (ตั้งรหัสผ่านใหม่ตรงๆ ไม่ต้องรู้รหัสเดิม, validate ความยาวขั้นต่ำ 8 ตัวอักษรเหมือนตอนสมัคร, hash ด้วย `hashPassword()` เดิม) — เพิ่ม store function `updateUserProfile`/`updateUserPassword` คู่ขนานทั้ง `server/db.js` และ `test/memoryStore.js` เหมือนแพทเทิร์นเดิมของโปรเจกต์ — ทดสอบ backend ครบใน `test/roles.test.js` (แก้โปรไฟล์สำเร็จ, ต้องเหลือ email/phone อย่างน้อย 1 อย่าง, validate format, เช็ค duplicate email/phone, non-admin เข้าไม่ได้ทั้งสอง endpoint, เปลี่ยนรหัสผ่านสำเร็จแล้ว login ด้วยรหัสใหม่ได้จริงและรหัสเก่าใช้ไม่ได้อีก, รหัสสั้นเกินไปโดนปฏิเสธ) ทดสอบ end-to-end ผ่านเบราว์เซอร์จริงครบ: ตาราง+ตัวกรองแสดงผลถูกต้อง (รวม enterprise_admin ที่ดูแลกลุ่มแต่ไม่ได้เป็นสมาชิก), เพิ่ม/แก้ไข/เปลี่ยนรหัสผ่านสำเร็จจริงและ login ด้วยรหัสใหม่ได้, validation ฝั่ง client (รหัสไม่ตรงกัน) ทำงานถูกต้อง — `npm run build --prefix admin` ผ่านไม่มี error, `npm test` ผ่านครบ 35 ข้อ
- **Login ด้วยเบอร์โทรศัพท์**: เพิ่มเป็นทางเลือกคู่กับอีเมล (ไม่ใช่แทนที่) — สมัคร/เพิ่มผู้ใช้กรอกได้ทั้งอีเมลและ/หรือเบอร์โทร แต่ต้องมีอย่างน้อย 1 อย่าง (`validateRegistration` ใน `server/app.js`) เบอร์โทรต้องเป็น 10 หลักขึ้นต้นด้วย 0 (`PHONE_RE`, ตัด `-`/space ออกด้วย `normalizePhone`) `users` table มี column `phone TEXT UNIQUE` เพิ่มมา และ `email` เปลี่ยนจาก `NOT NULL` เป็น nullable แล้ว (migration ใน `schema.sql` เป็น `ALTER TABLE` ที่รันซ้ำได้ปลอดภัยทุกครั้งที่บูต ทดสอบแล้วว่า migrate จาก schema เก่าไม่มีข้อมูลหาย) ฟอร์ม login เหลือช่องเดียวคือ "อีเมล หรือ เบอร์โทรศัพท์" (`POST /api/auth/login` รับ `{identifier, password}`) เซิร์ฟเวอร์เช็คด้วย `EMAIL_RE` ว่า identifier เป็นอีเมลหรือเบอร์โทรแล้วค้นตารางให้ถูก — ยังไม่มีการส่ง SMS OTP ยืนยันตัวตนใดๆ (ไม่มี SMS provider ต่อไว้) เป็นแค่ใช้เบอร์โทรแทนชื่อผู้ใช้เฉยๆ
- **(v1.44) หน้าแก้ไขวิสาหกิจชุมชน ป้อนรหัสไปรษณีย์แล้วค้นหา/เลือกที่อยู่ได้ (พอร์ตจากแอปภาคสนาม)**: เดิมฟอร์มแก้ไข `CommunityEnterpriseCard.jsx` มีแค่ช่องอำเภอ/จังหวัด/รหัสไปรษณีย์เป็น text input ล้วนๆ ต้องพิมพ์เอง ต่างจากแอปภาคสนามที่มี auto-fill จากรหัสไปรษณีย์มาตั้งแต่ v1.7 — พอร์ต logic เดิม (`loadThaiZipIndex()`/`handlePostcodeInputFor()` ใน `index.html`) มาเป็นโมดูล React `admin/src/thaiZip.js` (`loadThaiZipIndex()` เดียวกันทุกอย่าง โหลด+cache JSON เดียวกันจาก `raw.githubusercontent.com/kongvut/thai-province-data`, เก็บ index แบบ singleton module-level เหมือนเดิม) **ตัดระดับตำบลออก** เพราะ `community_enterprises` ไม่มีคอลัมน์ subdistrict (ต่างจากแปลงที่ดินที่มี) — dedup index แค่ระดับ (อำเภอ, จังหวัด) พอ — เอาช่อง `district`/`province`/`postcode` ออกจาก `FIELDS` array เดิม (ที่ render เป็น text input ธรรมดาในลูป) ย้ายมาเป็นบล็อกเฉพาะ "ที่ตั้ง" ด้านล่าง (ช่องรหัสไปรษณีย์ก่อน ตามด้วยข้อความสถานะ + dropdown เลือกที่อยู่ถ้าเจอมากกว่า 1 ที่ + ช่องอำเภอ/จังหวัดที่ auto-fill แล้วแต่ยังแก้ไขเองได้ถ้า auto-fill ผิดหรือหาไม่เจอ) — ใช้ React state (`addressStatus`, `addressMatches`) แทน DOM manipulation ตรงๆ แบบ vanilla JS เดิม, มี stale-request guard ผ่าน `useRef` (`postcodeRequestRef`) กันกรณีพิมพ์รัวๆ แล้ว response เก่ามาทับ response ใหม่ (เทียบเท่า logic เดิมใน index.html ที่เช็คค่า input ปัจจุบันก่อน apply ผล) — reset `addressStatus`/`addressMatches` ทุกครั้งที่เปิดโหมดแก้ไขใหม่ (`startEdit()`) กันสถานะเก่าค้างข้ามรอบเปิด-ปิดฟอร์ม — ทดสอบผ่านเบราว์เซอร์จริง: รหัสไปรษณีย์ที่มีที่อยู่เดียว (50200 เชียงใหม่) auto-fill อำเภอ/จังหวัดถูกต้องทันที, รหัสที่มีหลายที่อยู่ (10110 กรุงเทพฯ) ขึ้น dropdown ให้เลือก 2 ตัวเลือกถูกต้อง (รวมชื่อสไตล์ "เขต" ของกรุงเทพฯ) เลือกแล้ว fill ถูกต้อง บันทึกแล้วยืนยันค่าไปถึง server จริงและแสดงผลถูกต้องทั้งในตารางและ modal รายละเอียด เป็นงานฝั่ง admin frontend ล้วนๆ `npm run build --prefix admin` ผ่านไม่มี error, `npm test` (backend) ผ่านครบ 44 ข้อเหมือนเดิม (ไม่กระทบ)
- **(v1.43) หน้า admin "วิสาหกิจชุมชน" เปลี่ยนจากการ์ดเรียงต่อกันเป็นตาราง**: เดิม `CommunityEnterprises.jsx` แสดงทุกกลุ่มเป็น `CommunityEnterpriseCard` เต็มรูปแบบ (ฟอร์มแก้ไข+สมาชิก+แปลง+รออนุมัติ) เรียงต่อกันแนวตั้งทั้งหมด สโครลยาวถ้ามีหลายกลุ่ม — เปลี่ยนเป็นตารางสรุป (คอลัมน์: ชื่อกลุ่ม, วัตถุประสงค์หลัก, ที่ตั้ง, สมาชิก, แปลงที่อนุมัติ, รออนุมัติ [badge สีส้มถ้า >0], จัดการ) กดปุ่ม "จัดการ" ต่อแถวเปิด **modal ที่ห่อ `CommunityEnterpriseCard` เดิมไว้ทั้งตัวไม่ได้เขียนใหม่** (component ใหม่ `CommunityEnterpriseDetailModal` ใน `CommunityEnterprises.jsx` เอง ไม่ได้แยกไฟล์ต่างหากเพราะใช้ที่เดียว) — เลือกทางนี้แทนการยัดทุกอย่างลงในแถวตาราง เพราะ `CommunityEnterpriseCard` มีฟังก์ชันซับซ้อนเกินจะ inline ในแถวได้ (ฟอร์มแก้ไข 10+ ฟิลด์, จัดการสมาชิก, จัดการแปลง 3 สถานะ) การห่อด้วย modal แทนที่จะรื้อ component เดิมทำให้**ฟังก์ชันทั้งหมดที่เพิ่งสร้าง/ทดสอบใน v1.41-v1.42 (แก้ไขกลุ่ม, เพิ่ม/ลบสมาชิก, ผูก/ถอนแปลง, อนุมัติ/ปฏิเสธคำขอเข้าร่วม) ยังทำงานเหมือนเดิมทุกอย่างโดยไม่ต้องแก้โค้ดข้างในเลยแม้แต่บรรทัดเดียว** เปลี่ยนแค่จุดที่ครอบ/เรียกใช้ — ปุ่ม "ลบ" ย้ายไปอยู่ใน modal เท่านั้น (ในตัว `CommunityEnterpriseCard` เดิมมีอยู่แล้วพร้อม guard เดิม "ต้องไม่มีสมาชิกเหลือก่อนจึงลบได้") ไม่ได้เพิ่มปุ่มลบซ้ำในแถวตารางเพื่อไม่ต้องเขียน confirm/error-handling ซ้ำสองที่ — ทดสอบผ่านเบราว์เซอร์จริง: สร้าง 2 กลุ่มทดสอบ (กลุ่มหนึ่งมีวัตถุประสงค์+สมาชิก+แปลงอนุมัติแล้ว+แปลงรออนุมัติ, อีกกลุ่มว่างเปล่า) ยืนยันตารางแสดงตัวเลขสรุปถูกต้องครบทุกคอลัมน์ กดจัดการเปิด modal เห็นข้อมูลและปุ่มอนุมัติ/ปฏิเสธ/แก้ไข/ลบครบเหมือนก่อนเปลี่ยน กดปิด modal ทำงานถูกต้อง เป็นงานฝั่ง admin frontend ล้วนๆ `npm run build --prefix admin` ผ่านไม่มี error, `npm test` (backend) ผ่านครบ 44 ข้อเหมือนเดิม (ไม่กระทบ)
- **(v1.42) แปลงขอเข้าร่วมวิสาหกิจชุมชนต้องวัตถุประสงค์ตรงกัน + ต้องรออนุมัติ + อนุมัติแล้วได้เป็นสมาชิกอัตโนมัติ**: ต่อยอดจาก v1.41 — เดิมผูกแปลงเข้ากลุ่มได้อิสระผ่าน `PUT /api/plots/:id` (ไม่เช็ควัตถุประสงค์, ไม่มีขั้นตอนอนุมัติ) และสมาชิกภาพเป็นคนละเรื่องกับแปลงเลย (admin เพิ่ม/ลบสมาชิกตรงๆ) — ผู้ใช้ให้กฎใหม่ 2 ข้อ: (1) user คนเดียวเป็นสมาชิกได้หลายวิสาหกิจชุมชนพร้อมกัน (ยืนยันว่าเป็นแบบนี้อยู่แล้วตั้งแต่ต้น ไม่ต้องแก้) (2) 1 แปลงเข้าได้ 1 วิสาหกิจ และวัตถุประสงค์ต้องตรงกัน — ถามผู้ใช้ต่อจนได้ workflow เต็มรูปแบบ: field worker สร้าง/แก้ไขแปลงในแอปภาคสนาม เลือกวัตถุประสงค์ก่อน ระบบจะกรองแสดงเฉพาะวิสาหกิจชุมชนที่มีวัตถุประสงค์**ตรงกัน**ให้เลือกขอเข้าร่วม (ไม่บังคับ) → ส่งคำขอแล้วอยู่สถานะ "รออนุมัติ" → หน้า admin วิสาหกิจชุมชนของกลุ่มนั้นเป็นคนกดปุ่ม "อนุมัติ" → พอเช็คแล้วจะ**ทั้งอนุมัติแปลงเข้ากลุ่มและเพิ่มเจ้าของแปลงเป็นสมาชิกกลุ่มพร้อมกันในขั้นตอนเดียว** ("คนที่จะเข้าวิสาหกิจต้องมีแปลงของตัวเอง" — สมาชิกภาพไหลมาจากแปลงที่ผ่านอนุมัติเท่านั้น ไม่ใช่เพิ่มลอยๆ เหมือนเดิม แต่ปุ่มเพิ่มสมาชิกแบบเดิม — เลือกจาก user ที่ลงทะเบียนแล้วตรงๆ ไม่ผ่านแปลง — ยังเก็บไว้เป็นทางเลือกสำรอง ตามที่ผู้ใช้เลือกไว้ ไม่ได้ตัดออก)
  - **Schema**: `plots.community_enterprise_status` คอลัมน์ใหม่ (TEXT, nullable, ค่า `pending`|`approved`, ว่างเมื่อไม่ได้ผูกกลุ่มไหนเลย)
  - **Backend**: เพิ่ม public endpoint ใหม่ 2 ตัว (ไม่ต้อง login เพราะ field worker ทั่วไป role `user` ไม่มีสิทธิ์เรียก `/api/admin/*` อยู่แล้ว) — `GET /api/purposes` (list วัตถุประสงค์ทั้งหมด) และ `GET /api/community-enterprises` (คืนแค่ `id`/`name`/`purposeId` เท่านั้น ไม่ใช่ object เต็มแบบ admin เพื่อไม่ให้ข้อมูลอ่อนไหว เช่น เลขทะเบียน/เอกสาร รั่วออกไปสู่สาธารณะ) — `PUT /api/plots/:id` (public endpoint เดิม) เพิ่ม logic ใหม่: ถ้า `communityEnterpriseId` ที่ส่งมา**เปลี่ยนไปจากเดิม** (ผูกกลุ่มใหม่/ถอนกลุ่ม) เซิร์ฟเวอร์จะคำนวณ `communityEnterpriseStatus` เองเสมอ (ไม่สนใจค่าที่ client ส่งมาเลย แม้จะพยายามส่ง `'approved'` มาตรงๆ ก็ตาม — กันไม่ให้ endpoint สาธารณะถูกใช้ self-approve ได้) ผูกกลุ่มใหม่ → บังคับเป็น `'pending'` เสมอ + ตรวจสอบว่า `plot.purposeId === targetCE.purposeId` ก่อน (ทั้งคู่ต้องมีค่าตรงกันเป๊ะ **หรือทั้งคู่ไม่มีค่าเลยก็นับว่าตรงกัน** — จงใจออกแบบแบบนี้เพื่อไม่ทำให้เทสต์/ข้อมูลเก่าที่ยังไม่มีวัตถุประสงค์พังหมด) ไม่ตรงกันคืน `400`, ถอนกลุ่ม (`communityEnterpriseId: null`) → เคลียร์ status เป็น `null` ไปด้วย, แก้ไขฟิลด์อื่นโดยไม่แตะ `communityEnterpriseId` เลย → คง status เดิมไว้ไม่เปลี่ยน — endpoint ใหม่ `PATCH /api/admin/community-enterprises/:id/plots/:plotId/approve` (`requireAdminOrEnterpriseAdmin` + `isOwnCommunityEnterprise`) เซ็ต status เป็น `approved` แล้วเพิ่ม `plot.createdBy` (ถ้ามี) เป็นสมาชิกกลุ่มทันทีในคำสั่งเดียว คืนทั้ง plot ที่อัปเดตแล้วและ list สมาชิกล่าสุด — เพิ่ม `findCommunityEnterpriseById` helper ทั้ง `server/db.js`/`test/memoryStore.js` (เดิมมีแต่ `listCommunityEnterprises()` คืนทั้งหมด ต้องมาหาเองทีละตัว)
  - **แอปภาคสนาม (index.html)**: ทั้งฟอร์ม "+ เพิ่มแปลงใหม่" และฟอร์ม "แก้ไขข้อมูลแปลง" มีช่อง "วัตถุประสงค์" (select) ตามด้วยช่อง "ขอเข้าร่วมวิสาหกิจชุมชน" (select, ไม่บังคับ) ที่ **กรองอัตโนมัติให้เหลือเฉพาะกลุ่มที่วัตถุประสงค์ตรงกับที่เลือกไว้** (ถ้ายังไม่เลือกวัตถุประสงค์ ช่องนี้ disabled พร้อมคำแนะนำ) ใช้ฟังก์ชันกลาง `populatePurposeAndCeSelects()` ตัวเดียวกันทั้งสองฟอร์ม, cache รายการวัตถุประสงค์/วิสาหกิจชุมชนไว้ใน `cachedPurposes`/`cachedCommunityEnterprises` (fetch ครั้งเดียวต่อ session) — ฟอร์มแก้ไขแสดงสถานะปัจจุบันด้วยถ้ามีการผูกกลุ่มอยู่แล้ว ("🕐 รอกลุ่มอนุมัติการเข้าร่วม" สีน้ำตาล / "✅ เข้าร่วมกลุ่มแล้ว" สีเขียว) — **เจอบั๊กเล็กน้อยระหว่าง dev**: โค้ดเดิมหลังบันทึกแปลง (ทั้งตอนสร้างใหม่และแก้ไข) push/set `plots[idx]` ด้วย object ที่**สร้างขึ้นเองฝั่ง client** ไม่ใช่ response จริงจาก server ซึ่งไม่มีปัญหามาก่อนเพราะทุกฟิลด์ client รู้ค่าล่วงหน้าอยู่แล้ว แต่ `communityEnterpriseStatus` เป็นค่าที่**เซิร์ฟเวอร์เป็นคนคำนวณเอง** client ไม่มีทางรู้ค่าที่ถูกต้องล่วงหน้า ถ้าใช้ object ฝั่ง client จะโชว์สถานะผิด/ค้างจนกว่าจะ reload หน้า — แก้โดยเปลี่ยนทั้งสองจุด (`btn-save-boundary` ตอนสร้างแปลงใหม่, `btn-save-plot-detail` ตอนแก้ไข) ให้ใช้ **response จริงจาก `apiSavePlot()`** (ซึ่ง return `res.json()` อยู่แล้วแต่โค้ดเดิมทิ้งค่านี้ไป) แทน
  - **หน้า admin วิสาหกิจชุมชน**: `CommunityEnterpriseCard.jsx` แยกรายการแปลงเป็น 2 ส่วนตอนนี้: "🕐 รออนุมัติเข้าร่วม" (แปลงที่ `communityEnterpriseId===entity.id && communityEnterpriseStatus==='pending'`, มีปุ่ม "อนุมัติ"/"ปฏิเสธ") กับ "แปลงที่ดินในกลุ่ม" เดิม (ตอนนี้กรองเฉพาะ `communityEnterpriseStatus==='approved'` เท่านั้น ไม่ปนกับที่ยังรออนุมัติ) — dropdown "เลือกแปลงที่ยังไม่สังกัดกลุ่ม" (ที่ admin ผูกแปลงเข้ากลุ่มเองได้ตรงๆ) กรองเหลือเฉพาะแปลงที่วัตถุประสงค์ตรงกับกลุ่มด้วยเช่นกัน (`(p.purposeId||null) === (entity.purposeId||null)`) — ถามผู้ใช้แล้วว่า flow นี้ (admin ผูกเอง) ควร "ผูกและอนุมัติทันที ไม่ต้องรอ" เพราะ admin เป็นคนกดยืนยันเองอยู่แล้ว จึงทำ `linkPlot()` เป็น 2 คำสั่งต่อกัน (`savePlot` ให้ตกเป็น pending ตามกติกาเซิร์ฟเวอร์ปกติ แล้วเรียก approve endpoint ทันที) แทนที่จะเปิดช่องพิเศษให้ endpoint ปกติ bypass ได้ — ปุ่มเพิ่มสมาชิกแบบเดิม (เลือกจาก user ที่ลงทะเบียนแล้วตรงๆ ไม่ผ่านแปลง) เก็บไว้เป็นทางเลือกสำรองตามที่ผู้ใช้ตอบ ไม่ได้ตัดออก
  - **placeholder**: ทุกจุดที่แสดง/เลือกวัตถุประสงค์แล้วยังไม่มีค่า ใช้ข้อความ "ยังไม่กำหนดวัตถุประสงค์" ให้ตรงกันหมด (option แรกของทุก select ที่เกี่ยวกับวัตถุประสงค์ + ข้อความแสดงผลตอนไม่มีค่าใน `PlotsTable.jsx`/`CommunityEnterpriseCard.jsx`)
  - ทดสอบ backend ครบใน `test/joinRequests.test.js` (endpoint public ไม่ต้อง auth, วัตถุประสงค์ไม่ตรงโดนปฏิเสธ 400 ทั้งกรณีต่างกันและกรณีแปลงไม่มีวัตถุประสงค์เลย, ผูกสำเร็จได้แค่สถานะ pending แม้ client จะพยายามส่ง approved มาตรงๆ ก็ถูกเซิร์ฟเวอร์เขียนทับ, แก้ไขฟิลด์อื่นไม่กระทบ status เดิม, non-owner enterprise_admin อนุมัติไม่ได้ 403, อนุมัติสำเร็จแล้วเช็คได้ทั้ง plot.status เปลี่ยนและสมาชิกภาพเพิ่มขึ้นจริง, ถอนกลุ่มเคลียร์ status, **ยืนยัน backward compatible กับเทสต์/พฤติกรรมเดิมทั้งหมดที่ผูกแปลง-กลุ่มแบบไม่มีวัตถุประสงค์เลยยังผ่านได้ตามปกติ**) ทดสอบ end-to-end ผ่านเบราว์เซอร์จริงครบวงจร: field worker สร้างแปลงเลือกวัตถุประสงค์+ขอเข้าร่วมกลุ่มที่ตรงกัน → เห็นสถานะ pending ทั้งตอนสร้างและตอนเปิดแก้ไขทีหลัง → admin เห็นคำขอในส่วน "รออนุมัติ" → กดอนุมัติ → สมาชิกเพิ่มอัตโนมัติ + แปลงย้ายไปอยู่ "แปลงที่ดินในกลุ่ม" → field worker เปิดแก้ไขแปลงอีกครั้งเห็นสถานะเปลี่ยนเป็น "✅ เข้าร่วมกลุ่มแล้ว" ถูกต้องทุกขั้นตอน — `npm run build --prefix admin` ผ่านไม่มี error, `npm test` ผ่านครบ 44 ข้อ
- **(v1.41) ตารางวัตถุประสงค์ (lookup table) + ผูกกับแปลงและวิสาหกิจชุมชนได้ 1 ข้อต่อรายการ**: ตาราง `purposes` ใหม่ (`id`, `name`) จัดการ CRUD (เพิ่ม/แก้ไข/ลบ) ได้จากหน้า admin ใหม่ "วัตถุประสงค์" ในเมนู sidebar (`adminOnly:true` เหมือน "ผู้ใช้งาน" — ซ่อนจาก nav และ block route ตรงด้วย `ProtectedRoute requireAdmin` ถ้า `enterprise_admin` พยายามเข้า URL ตรงๆ) endpoint `GET /api/admin/purposes` เปิดให้ทั้ง `admin`/`enterprise_admin` อ่านได้ (`requireAdminOrEnterpriseAdmin`, จำเป็นเพราะทั้งสอง role ต้องเลือกวัตถุประสงค์จากรายการนี้ได้) แต่ `POST`/`PUT`/`DELETE` จำกัดแค่ `admin` เท่านั้น (`requireAdmin`) — ตัดสินใจตามที่ผู้ใช้เลือกไว้ชัดเจนหลังถามยืนยัน 3 ข้อ: (1) `plots`/`community_enterprises` เพิ่มคอลัมน์ `purpose_id` (FK ไป `purposes.id`, `ON DELETE SET NULL` — ลบวัตถุประสงค์ทิ้งแล้วแปลง/กลุ่มที่เคยเลือกไว้จะกลับเป็นไม่ระบุ ไม่ error ไม่ลบข้อมูลอื่นตามไปด้วย ทดสอบยืนยันแล้วทั้งฝั่ง memory store และ endpoint จริง) เลือกได้ **ข้อเดียวต่อแปลง/ต่อกลุ่ม** (ไม่ใช่ multi-select) (2) วิสาหกิจชุมชนที่มีช่อง "วัตถุประสงค์" เป็น free text อยู่แล้วตั้งแต่เดิม **ไม่ได้ถูกแทนที่** — คอลัมน์ `purpose` (free text) เดิมยังอยู่ครบ แค่เปลี่ยนป้ายกำกับ (label) ในฟอร์มแก้ไขจาก "วัตถุประสงค์/ประเภทกิจการ" เป็น "รายละเอียดวัตถุประสงค์" ส่วนช่องใหม่ (`purposeId`, dropdown เลือกจากตาราง) ใช้ label "วัตถุประสงค์หลัก" — ทั้งสองช่องแสดงคู่กันทั้งตอนแก้ไขและตอนดูอย่างเดียว (🎯 ชื่อวัตถุประสงค์หลัก ต่อด้วยรายละเอียด free text ถ้ามี) (3) จัดการรายการวัตถุประสงค์จากหน้า admin ใหม่ตามที่กล่าวไปข้างต้น — ฝั่งแปลง: `PlotsTable.jsx` เพิ่มคอลัมน์ "วัตถุประสงค์" เป็น `<select>` ตอนแก้ไข (ไม่ใช่ text input เหมือนฟิลด์อื่นใน `FIELDS` เพราะต้องเลือกจาก lookup ไม่ใช่พิมพ์เอง) แสดงชื่อวัตถุประสงค์ (resolve จาก id ผ่าน `purposesById` Map) ตอนไม่ได้แก้ไข — `Plots.jsx`/`CommunityEnterprises.jsx` เพิ่ม fetch `api.listPurposes()` เข้า `reload()` ของแต่ละหน้าเอง (คนละ state ไม่ share ข้ามหน้า ตามแพทเทิร์นเดิมของทั้งแอป) — **endpoint เดิม `PUT /api/plots/:id` และ `PUT /api/admin/community-enterprises/:id` ไม่ต้องแก้เลย** เพราะทั้งคู่ spread `req.body` เข้า object ที่จะ upsert อยู่แล้ว (`{...req.body, id, name}`) `purposeId` เลยไหลผ่านไปเองอัตโนมัติ ต้องแก้แค่ `server/db.js` (`plotRowToObj`/`upsertPlot`, `communityEnterpriseRowToObj`/`upsertCommunityEnterprise` เพิ่มคอลัมน์ `purpose_id`) — **ขอบเขตงานนี้เป็นฝั่ง admin เท่านั้น** แอปภาคสนาม (`index.html`) ไม่มี UI ให้เลือกวัตถุประสงค์ (field app ยังคง spread `{...plot, ...}` ตอนแก้ไขแปลงเหมือนเดิม จึงไม่ทำให้ `purposeId` ที่ admin ตั้งไว้หายไปเวลามีคนแก้ไขแปลงผ่านแอปภาคสนามทีหลัง — ค่าคงอยู่เพราะเป็น spread ไม่ใช่ overwrite) ทดสอบ backend ครบใน `test/purposes.test.js` (auth ครบ 401/403, CRUD ปกติ, ผูก+ลบแล้ว unlink ถูกต้องทั้งแปลงและวิสาหกิจชุมชน, enterprise_admin อ่านได้แต่เขียนไม่ได้) ทดสอบ end-to-end ผ่านเบราว์เซอร์จริงครบ: เพิ่ม 3 วัตถุประสงค์ตัวอย่าง (เพื่อ carbon credit/เพื่อกลุ่มไร่อ้อย/เพื่อกลุ่มผลไม้), ตั้งวัตถุประสงค์ให้แปลงทดสอบผ่านตาราง ยืนยันบันทึกถูกต้อง, ตั้งทั้งวัตถุประสงค์หลัก+รายละเอียดให้วิสาหกิจชุมชนทดสอบ ยืนยันแสดงผลถูกต้องทั้งสองช่องแยกกัน, ยืนยัน enterprise_admin เรียก API list ผ่านได้แต่ POST สร้างใหม่โดน 403 ตรงตามสิทธิ์ที่ออกแบบไว้ — `npm run build --prefix admin` ผ่านไม่มี error, `npm test` ผ่านครบ 39 ข้อ
- **วิสาหกิจชุมชน**: entity ใหม่ทั้งหมด เพิ่ม/แก้ไข/ดู/ลบได้จากหน้า `/admin/community-enterprises` (ลิงก์จากปุ่มบนหน้า dashboard) — ตาราง `community_enterprises` (ชื่อกลุ่ม, เลขทะเบียน, อำเภอ/จังหวัด/รหัสไปรษณีย์, วันที่จดทะเบียน, ประธาน/ผู้แทนกลุ่ม, เบอร์ติดต่อ, วัตถุประสงค์, เอกสารจดทะเบียนแบบรูป/base64) สมาชิกเป็นความสัมพันธ์ many-to-many ผ่านตาราง `community_enterprise_members` (FK ไปยัง `users` — **เพิ่มสมาชิกได้เฉพาะจาก user ที่ลงทะเบียนในระบบแล้วเท่านั้น** ไม่มีการกรอกข้อมูลสมาชิกอิสระ) user คนเดียวเป็นสมาชิกได้หลายกลุ่มพร้อมกัน แปลงที่ดิน (`plots`) เชื่อมกับกลุ่มได้แบบ 1 แปลงต่อ 1 กลุ่ม ผ่าน column `community_enterprise_id` (nullable, `ON DELETE SET NULL`) — ผูก/ถอนแปลงทำผ่าน endpoint เดิม `PUT /api/plots/:id` (ส่ง `communityEnterpriseId` เพิ่มเข้าไปในตัว payload ที่มีอยู่แล้ว ไม่ได้เปิด endpoint ใหม่ ดังนั้นยังคงเป็น public เหมือน field อื่นๆ ของ plot) **กฎการลบ**: ลบกลุ่มได้ก็ต่อเมื่อไม่มีสมาชิกเหลืออยู่แล้วเท่านั้น (เช็คที่ทั้ง server — คืน `409` ถ้ายังมีสมาชิก — และ client ที่ปิดปุ่มลบไว้ก่อน) ไม่เกี่ยวกับจำนวนแปลงที่ยังผูกอยู่ ทุก endpoint (`GET/PUT/DELETE /api/admin/community-enterprises`, `POST/DELETE .../members`) ต้อง `requireAuth`

## บั๊กที่เคยเจอและแก้แล้ว

**แผนที่ไม่แสดงผลบน Chrome มือถือ (แก้ใน commit `b4b0376`)**: มี CSS selector `#map-wrap` ประกาศซ้ำ 2 ที่ในไฟล์เดียวกัน กฎที่สอง (`flex:1;position:relative;` ซึ่งเป็นเศษซากจาก layout แบบ flex ที่เคยใช้) มาทีหลังจึงเขียนทับกฎแรกที่ถูกต้อง (`position:absolute` เต็มจอ) ทำให้ `#map-wrap`/`#map` ยุบเหลือ 0 ความสูง และปุ่ม GPS (ลูกของ `#map-wrap` ที่ใช้ `position:absolute`) หลุดออกนอกจอ เป็นบั๊ก CSS cascade ล้วนๆ ไม่มี JS exception จึงไม่มี error banner ขึ้นเตือน — ถ้าเจออาการคล้ายกัน (หน้าจอว่างเปล่าแบบไม่มี error เลย) ให้สงสัย CSS selector ซ้ำ/ชนกันก่อนสงสัยว่าโหลด library ไม่สำเร็จ

**รูปแปลงที่ถ่ายแล้วดูเหมือนไม่มีข้อมูลประทับ (พรีวิวในแอปเท่านั้น ตัวข้อมูลจริงถูกบันทึกถูกต้องเสมอ)**: `.plot-photo-preview` (ใช้แสดงทั้ง `plot.photo` และ `plot.docPhoto`) เดิมใช้ `object-fit:cover` ในกล่องที่กว้างมากแต่เตี้ย (`width:100%;max-height:220px`) รูปที่ `stampPlotPhoto()` ประทับข้อมูลมักเป็นแนวตั้ง (portrait) — `cover` เลยขยายรูปจนกว้างเต็มกล่องแล้ว **crop ส่วนบน-ล่างทิ้งไปเกือบหมด** ซึ่งพอดีเป็นตำแหน่งที่ชื่อเจ้าของ (มุมบนซ้าย) และที่อยู่/พิกัด GPS (มุมล่าง) ถูกวาดไว้ ทำให้ preview เหลือแต่ส่วนกลางของรูป (พื้น/ท้องฟ้าเฉยๆ) ดูเหมือนไม่มีข้อมูลทั้งที่ base64 ที่บันทึกลง DB มีข้อมูลครบ (ตรวจสอบแล้วว่า `img.src` ในหน้าตรงกับ `plot.photo` เป๊ะ) แก้โดยเปลี่ยนเป็น `object-fit:contain` (ไม่ crop, ยอมมี letterbox) และเพิ่ม `max-height` เป็น 420px — ถ้าเจออาการ "ข้อมูลหาย" ในรูปอีกให้สงสัย CSS การแสดงผล (`object-fit`/crop) ก่อนสงสัย logic การประทับข้อมูลใน canvas

## Backend / Deploy

- `server.js` = entry point จริงที่รันบน Render (`npm start`) อ่าน `DATABASE_URL` และ `JWT_SECRET` จาก env (fail fast ถ้าขาดอันใดอันหนึ่ง), รัน `schema.sql` ตอนบูต (retry ~10 ครั้ง เผื่อ Postgres ยังไม่พร้อมตอน deploy พร้อมกัน) แล้วค่อย listen
- `server/db.js` = `createPgStore(connectionString)` คุย Postgres จริงผ่าน `pg` (parameterized query, upsert ด้วย `ON CONFLICT`) แปลง row snake_case ↔ object camelCase ให้ตรงกับ shape เดิมที่ client ใช้อยู่แล้ว มีทั้ง CRUD ของ plots/trees, users (`createUser`, `findUserByEmail`, `findUserById`) และ community enterprises (`listCommunityEnterprises`, `upsertCommunityEnterprise`, `deleteCommunityEnterprise`, `countCommunityEnterpriseMembers`, `listCommunityEnterpriseMembers`, `addCommunityEnterpriseMember`, `removeCommunityEnterpriseMember`) — `test/memoryStore.js` (in-memory store สำหรับ dev/test) implement ฟังก์ชันชุดเดียวกันทั้งหมดขนานกัน ต้องแก้พร้อมกันทุกครั้งที่เพิ่ม/แก้ไข store function
- `server/auth.js` = hash รหัสผ่านด้วย `bcryptjs`, ออก session เป็น JWT เก็บใน httpOnly cookie (`tm_session`, อายุ 7 วัน) ผ่าน `jsonwebtoken` + `cookie-parser` — `requireAuth` middleware ใช้ป้องกันเฉพาะ route ที่ต้อง login
- `server/export.js` = แปลง plots/trees เป็น CSV (`plotsToCSV`, `treesToCSV`) และ GeoJSON FeatureCollection (`toGeoJSON`, polygon จาก boundary + point จากต้นไม้)
- `server/app.js` = `createApp(store)` คืน Express app — รับ store แบบ dependency-injected ตั้งใจให้ swap ระหว่าง pg store จริงกับ store อื่นได้ (ใช้ทดสอบโดยไม่ต้องมี Postgres จริง) มี route: `/api/plots`, `/api/trees` (public), `/api/auth/register|login|logout|me`, `/api/admin/export/plots.csv|trees.csv|geojson` (ต้อง `requireAuth`), และ serve static `/admin` จาก `admin/dist` ถ้ามี (เช็คด้วย `fs.existsSync` — ถ้ายังไม่ build ไว้ route `/admin` จะไม่ถูกลงทะเบียนเลย ไม่ error)
- `admin/` = โปรเจกต์ React แยก (Vite, มี `package.json`/`node_modules`/`dist` ของตัวเอง) — React Router ใช้ `basename="/admin"`, ทุก fetch ใส่ `credentials:'include'` เพื่อส่ง cookie session, ใช้ CircleMarker แทน default Leaflet marker icon เพื่อเลี่ยงปัญหา asset path ตอน bundle **สำคัญ**: `admin/index.html` ต้องมี `<html lang="th">` ไม่ใช่ `lang="en"` — เจอบั๊กจริงว่าถ้าใช้ `lang="en"` ทั้งที่เนื้อหาในหน้าเป็นภาษาไทยล้วน Chrome จะเปิด auto-translate แล้วแปลข้อความไทยที่ถูกอยู่แล้วให้กลายเป็นคำผิดเพี้ยน (เช่น "เจ้าของ" → "สิ่ง") เจออาการนี้ที่ไหนอีกให้เช็ค `lang` attribute ก่อน
- `render.yaml` = Blueprint สร้าง Web Service + Postgres database ให้พร้อมกันตอน deploy (ผู้ใช้กด "New Blueprint" บน Render แล้วเชื่อม repo นี้เอง) `buildCommand: npm install && npm run build` (root `build` script สั่ง build `admin/` ด้วย) มี env var `JWT_SECRET` แบบ `generateValue: true` ให้ Render สุ่มให้อัตโนมัติ
- **Local dev**: `npm run dev` ใช้ in-memory store (`scripts/dev-memory-server.js`, `test/memoryStore.js`) ไม่ต้องมี Postgres ในเครื่อง (auto-set `JWT_SECRET` ชั่วคราวให้) — เครื่องนี้บังเอิญมี PostgreSQL 18 ติดตั้งจริงด้วย (Windows service `postgresql-x64-18`) เคยใช้ต่อทดสอบแบบเต็มรูปแบบผ่าน `psql`/`server.js` จริงแล้ว (ยืนยัน UTF-8 ภาษาไทย round-trip ถูกต้อง, cascade delete ทำงานถูกต้อง) ถ้าต้องการรันแบบนี้อีกต้องขอรหัสผ่าน `postgres` user จากผู้ใช้ในแชท (อย่าเดา/จำรหัสผ่านไว้ในไฟล์ใดๆ ในโปรเจกต์) — **ระวัง**: ระหว่าง dev เจอปัญหา stray Node process ค้าง listen port 8934 จากรอบทดสอบก่อนหน้าหลายครั้ง (เพราะ background process ผ่าน bash ไม่ได้ถูก kill จริงข้าม tool call) ถ้า route ใหม่ที่เพิ่งเขียน 404 อยู่ทั้งที่โค้ดถูกแล้ว ให้เช็ค `netstat -ano | grep :8934` หา PID เก่าค้างอยู่ก่อน
- **Testing**: `npm test` รัน `test/api.test.js`, `test/auth.test.js` (in-memory, รันได้เสมอ) และ `test/api.pg.test.js` (skip อัตโนมัติถ้าไม่ตั้ง `TEST_DATABASE_URL` — ตั้งไว้เพื่อรันซ้ำกับ Postgres จริงได้ถ้าต้องการความมั่นใจเพิ่ม)

## แผนในอนาคต (คุยไว้ ยังไม่เริ่มทำ)

ผู้ใช้ต้องการขยายเป็นระบบเต็มรูปแบบสำหรับ carbon credit: แอปมือถือสำหรับเจ้าหน้าที่ภาคสนาม (GPS + ชนิดพันธุ์ + สุขภาพต้นไม้ + รูปภาพ + ประวัติการดูแล) และเว็บแดชบอร์ด admin สำหรับตรวจสอบ/ส่งออกข้อมูล

- **Backend**: เดิมคุยกันไว้ว่าจะใช้ Supabase (Postgres + PostGIS) แต่ตอนนี้เปลี่ยนมาใช้ **Render + self-hosted Express/Postgres จริงแล้ว** (v1.8) ยังไม่ได้ใช้ PostGIS (เก็บพิกัดเป็น column ธรรมดา/JSONB) — ถ้าต้อง query เชิงพื้นที่ซับซ้อนขึ้น (เช่น หาแปลงที่ทับซ้อนกัน) ค่อยพิจารณาเพิ่ม extension `postgis` บน Render Postgres ทีหลัง
- Mobile: ยังไม่เริ่ม — แผนเดิมคือ Expo (React Native) พร้อม offline resilience แบบพื้นฐาน (คิว local ผ่าน `expo-sqlite` กันข้อมูลหายตอนอับสัญญาณ ไม่ใช่ offline-first เต็มรูปแบบ เพราะพื้นที่ทำงานปกติมีเน็ต) เมื่อเริ่มทำควรต่อ REST API เดียวกับที่ `index.html` ใช้อยู่ตอนนี้ (`/api/plots`, `/api/trees`) ได้เลย ไม่ต้องเขียน backend ใหม่
- Web admin: **เสร็จแล้ว** ที่ `/admin` (React + Vite + Tailwind + react-leaflet ตามแผนเดิม) — ดูรายละเอียดที่หัวข้อ "Backend / Deploy"

ฟีเจอร์ถ่ายรูปแปลง+ประทับข้อมูล (v1.5 ใน `index.html`) ควรย้ายไปทำใน mobile app (Expo) ด้วยเมื่อเริ่มพัฒนาจริง โดยแนวคิดเดิมคือ: ถ่ายรูป → อ่าน GPS ปัจจุบัน → วาด overlay ข้อความ (ชื่อเจ้าของ, เอกสารสิทธิ์, เนื้อที่, ที่อยู่, วันเวลา, พิกัด UTM) ลงบนรูปแบบเดียวกับแอป "GPS Map Camera" — บน mobile ควรใช้ `expo-camera` + `expo-location` แล้ววาด overlay ด้วย `react-native-view-shot` หรือ canvas เทียบเท่า แทนการใช้ `<canvas>` ของเว็บ ฟังก์ชันแปลง lat/lon เป็น UTM ที่เขียนไว้ใน `index.html` (`latLonToUTM`) พอร์ตไปใช้ตรงๆ ได้เลยเพราะเป็น pure JS ไม่พึ่ง DOM/browser API
